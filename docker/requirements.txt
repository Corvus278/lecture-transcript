# Runtime-зависимости пайплайна lecture-transcript, ставятся с PyPI.
#
# Ставить ПОСЛЕ requirements-torch.txt (torch/torchvision/torchaudio с индекса
# PyTorch) и ДО/ПОСЛЕ requirements-paddle.txt — см. docker/Dockerfile.
#
# Все версии проверены по https://pypi.org/pypi/<pkg>/json (наличие релиза и
# колёс cp311 для linux x86_64). Верхняя граница всего стека задана
# gigaam==0.1.0 -> torch<=2.5.1, поэтому набор выровнен на релизы,
# современные torch 2.5.1 (осень 2024 — весна 2025).
#
# Точка ревизии: после первой успешной сборки на целевой машине снять полный
# лок (`pip freeze > requirements.lock.txt`) — здесь пинуются прямые
# зависимости и рискованные транзитивные, но не весь граф.

# --------------------------------------------------------------------------
# База: массивы, изображения, конфиги (совпадает с pyproject.toml)
# --------------------------------------------------------------------------
# numpy 1.26.x, а не 2.x: paddlex требует numpy>=1.24, timm 0.5.4 и
# x-transformers 0.15.0 — библиотеки эпохи numpy 1.x; 1.26.4 удовлетворяет
# всем участникам сразу и не требует numpy2-совместимых сборок C-расширений.
numpy==1.26.4
Pillow==10.4.0
PyYAML==6.0.2          # жёстко пинится paddlex: PyYAML==6.0.2
ImageHash==4.3.2       # D4: perceptual hash для дедупликации кадров
PyWavelets==1.7.0      # транзитивная зависимость ImageHash, пинуется явно
scipy==1.14.1
pandas==2.2.3          # нужен и paddlex (>=1.3), и pix2tex (>=1.0)
tqdm==4.67.1
protobuf==4.25.5       # onnx>=3.20.2 и paddlepaddle>=3.20.2 — общий знаменатель

# OpenCV. Внимание: три пакета экосистемы поставляют один и тот же модуль `cv2`.
#   - opencv-python-headless просят pix2tex и albumentations
#   - opencv-contrib-python==4.10.0.84 жёстко пинит paddlex[ocr-core]
# Обе версии выровнены на 4.10.0.84, а в Dockerfile последним шагом
# принудительно переустанавливается contrib-сборка — чтобы победитель в `cv2`
# был детерминированным, а не зависел от порядка резолва pip.
# Не-headless сборке нужны libGL/libgthread — они ставятся apt-ом в образе.
opencv-python-headless==4.10.0.84
opencv-contrib-python==4.10.0.84

# --------------------------------------------------------------------------
# speech-transcription: VAD
# --------------------------------------------------------------------------
# 5.1.2, а не 6.x: ветка 6.x вышла заметно позже torch 2.5.1, а 5.1.2 —
# современница пинованного torch. D7: ~0.05 GB VRAM.
silero-vad==5.1.2

# --------------------------------------------------------------------------
# speech-transcription: ASR (D6)
# --------------------------------------------------------------------------
# GigaAM-v2 — бэкенд по умолчанию для русского.
# requires_dist пакета gigaam==0.1.0 (проверено по pypi.org/pypi/gigaam/0.1.0/json):
#   hydra-core<=1.3.2, numpy, omegaconf<=2.3.0, pydub<=0.25.1,
#   sentencepiece<=0.2.0, torch<=2.5.1, torchaudio<=2.5.1,
#   onnx==1.17.0, onnxruntime==1.17.3, tqdm
#   (+ extra "longform": pyannote.audio, pydub — не ставим)
# Все его пины продублированы ниже явно, чтобы они были видны при чтении файла,
# а не всплывали неожиданно при резолве.
gigaam==0.1.0
hydra-core==1.3.2
omegaconf==2.3.0
pydub==0.25.1
sentencepiece==0.2.0
onnx==1.17.0
onnxruntime==1.17.3    # CPU-сборка: используется только для VAD/служебных графов

# Whisper large-v3 через CTranslate2 — бэкенд для смешанной ru+en речи.
# ctranslate2 4.6.3 выбран сознательно: начиная с 4.5.0 CT2 работает
# с cuDNN 9 и несовместим с cuDNN 8 (release notes CTranslate2 v4.5.0),
# в 4.6.1 добавлена поддержка CUDA 12.4, в 4.6.3 — CUDA 12.8 и cuDNN стал
# опциональным. Базовый образ даёт CUDA 12.6 + cuDNN 9 — попадаем в середину
# поддерживаемого диапазона.
faster-whisper==1.2.1
ctranslate2==4.6.3
av==13.1.0
tokenizers==0.21.0     # диапазон задан transformers 4.49.x: >=0.21,<0.22
huggingface-hub==0.26.5  # ветка 0.x намеренно: hub 1.x ломает API transformers 4.x

# --------------------------------------------------------------------------
# speech-transcription: восстановление пунктуации и регистра (D6)
# --------------------------------------------------------------------------
# transformers 4.49.0, а не 4.46.3: только с 4.49.0 в пакете появился
# `qwen2_5_vl`, нужный альтернативному OCR-бэкенду (задача 4.8).
# Проверено по исходникам на GitHub:
#   .../transformers/v4.48.3/src/transformers/models/qwen2_5_vl/__init__.py -> 404
#   .../transformers/v4.49.0/src/transformers/models/qwen2_5_vl/__init__.py -> 200
# С torch 2.5.1 совместим: transformers не пинит torch сверху.
transformers==4.49.0

# Пунктуатор для русского. Единственный зарегистрированный бэкенд стадии 5.6 —
# SbertPuncCaseRu, он импортирует пакет `sbert_punc_case_ru`.
# На PyPI пакета НЕТ (404), репозитория github.com/kontur-ai/sbert_punc_case_ru
# тоже нет — код лежит внутри репозитория модели на HuggingFace.
# Проверено на закреплённом коммите f778dc6c:
#   - api.huggingface.co/.../revision/f778dc6c... -> 200, sha совпадает
#   - в дереве есть setup.py и пакет sbert_punc_case_ru/
#   - setup.py: name="sbert_punc_case_ru", version="0.2",
#     install_requires=["transformers>=4.36.2", "torch", "numpy"] — конфликта
#     с пинами выше нет
# Ставить с GIT_LFS_SKIP_SMUDGE=1 (см. Dockerfile): в том же репозитории лежит
# model.safetensors на ~1.7 GB, и без этого он утянется в контекст сборки.
# Веса нужны в рантайме и качаются в /cache, а не в образ.
# ВНИМАНИЕ: setup.py использует `distutils`, удалённый в Python 3.12.
# На Python 3.11 (наш случай) работает; при переезде на 3.12+ пин сломается.
sbert_punc_case_ru @ git+https://huggingface.co/kontur-ai/sbert_punc_case_ru@f778dc6c63bb0ec235a220488862509810e54583

# --------------------------------------------------------------------------
# slide-ocr: печатный текст (D5)
# --------------------------------------------------------------------------
# paddleocr 3.2.0 выбран вместо 3.0.x/3.1.x, потому что только начиная с 3.2.0
# зависимость на paddlex ограничена сверху (`paddlex[ocr-core]<3.3.0,>=3.2.0`)
# и использует лёгкий extra `ocr-core` вместо `[ie,multimodal,ocr]` — это
# и воспроизводимее, и в разы меньше по графу зависимостей.
# Русский берётся параметром lang="ru" (cyrillic-модели распознавания).
# Сам paddlepaddle-gpu ставится отдельно, см. requirements-paddle.txt.
paddleocr==3.2.0
paddlex[ocr-core]==3.2.0
modelscope==1.28.0     # транзитивная зависимость paddlex, пинуется явно
aistudio-sdk==0.3.5    # транзитивная зависимость paddlex, пинуется явно

# --------------------------------------------------------------------------
# slide-ocr: печатные формулы -> LaTeX (D5)
# --------------------------------------------------------------------------
# pix2tex 0.1.4 жёстко пинит timm==0.5.4 и x-transformers==0.15.0 —
# дублируем явно, чтобы пины были видны при чтении файла.
pix2tex==0.1.4
timm==0.5.4
x-transformers==0.15.0
einops==0.8.0
munch==4.0.0
albumentations==1.4.24  # верхняя граница из pix2tex (<=1.4.24)
albucore==0.0.23        # транзитивная зависимость albumentations 1.4.24
pydantic==2.9.2         # требуется и albumentations (>=2.9.2), и paddlex (>=2)

# --------------------------------------------------------------------------
# slide-ocr: альтернативный VLM-бэкенд Qwen2.5-VL 4bit (D5, задача 4.8)
# --------------------------------------------------------------------------
# Сам класс Qwen2_5_VLForConditionalGeneration приходит из transformers 4.49.0
# (пин выше). Для 4-битной загрузки нужны ещё два пакета:
#   bitsandbytes — квантизация NF4; колесо py3-none-manylinux_2_24_x86_64,
#                  ветка 0.45.x собрана под CUDA 12.4 — совпадает с torch +cu124
#   accelerate   — без него не работает device_map="auto" в vlm_backend.py
# Бэкенд по умолчанию НЕ используется (D5: ~6 GB VRAM против ~0.6 GB у пары
# PaddleOCR+pix2tex), но задача 4.8 требует, чтобы он был работоспособен.
bitsandbytes==0.45.5
accelerate==1.4.0
