# Хронология решений — реализация `lecture-transcript-pipeline`

Ведётся автономно во время выполнения `/opsx:apply`. Читать сверху вниз.

---

## 0. Стартовые условия (обнаружено)

| Что | Факт |
|---|---|
| Репозиторий | пустой, только `openspec/` и `.claude/`. Код с нуля |
| Платформа исполнения | **macOS (darwin, arm64)**, а не WSL2+Docker из design |
| GPU | **нет CUDA**, `nvidia-smi` отсутствует |
| ffmpeg | 8.1.1 есть, `videotoolbox`/CPU; nvdec недоступен |
| docker | 29.4.0 есть, но без NVIDIA runtime |
| Эталонная запись | **найдена**: `~/Downloads/wr_20260909_1150.mp4` |
| Python | 3.13.4 (pyenv), `uv` есть |

### Решение 0.1 — что делать с расхождением платформы

Целевая платформа по design — WSL2+Docker+RTX 4060. Разработка идёт на macOS.
Принято: **писать переносимый код, GPU-специфику держать за конфигом с CPU-фолбэком.**

- `hwaccel` — параметр, а не хардкод (`cuda` / `videotoolbox` / `none`);
  на macOS проверяем CPU-путь, на целевой машине включается `cuda`.
- Задачи, где проверка требует физически RTX 4060 (1.1, 1.2 частично, 7.2, 7.4)
  или скачивания многогигабайтных весов, **не отмечаются как выполненные** —
  артефакт создаётся, проверка помечается как невыполнимая здесь. См. раздел «Не проверено».

### Решение 0.2 — контракты пишутся в главной сессии, реализация — параллельно

Чтобы параллельные сабагенты не разъехались по типам, весь обмен данными между
стадиями зафиксирован заранее в `src/lecture_transcript/contracts.py`
(dataclasses + `Protocol` для `OcrBackend` / `AsrBackend`). Сабагентам запрещено
его править — только докладывать о необходимости изменения.

Границы владения: каждый сабагент пишет только в свой пакет
`src/lecture_transcript/<capability>/` и свои файлы тестов. `pyproject.toml`
и `contracts.py` — за главной сессией.

### Решение 0.3 — git

`git init` выполнен (репозитория не было) — чтобы аудит мог смотреть диффы.
Коммиты не делаются без явной просьбы.

### Решение 0.4 — маркеры pytest вместо пропуска проверок

Введены маркеры `reference` (нужна эталонная запись), `gpu`, `models`, `slow`.
Тесты, требующие тяжёлых ресурсов, пишутся, но не гоняются в общем прогоне.
Это позволяет проверить их на целевой машине, не размывая критерий готовности.

---

## 1. План распараллеливания

Волна 1 (7 сабагентов одновременно, границы владения по каталогам):

| Агент | Задачи | Владеет файлами |
|---|---|---|
| infra | 1.1, 1.2 | `docker/` |
| core | 1.3, 1.4 | `cli.py`, `config.py`, `cache.py`, `logging_setup.py`, `default_config.yaml` |
| fixtures | 1.5 | `tools/make_fixtures.py`, `tests/conftest.py`, `tests/fixtures_manifest.json` |
| media-ingest | 2.1–2.4 | `src/.../media_ingest/` |
| slide-ocr | 4.1–4.9 | `src/.../slide_ocr/` |
| speech-transcription | 5.1–5.8 | `src/.../speech_transcription/` |
| transcript-assembly | 6.1–6.6 | `src/.../transcript_assembly/` |

Волна 2: `slide-extraction` (3.1–3.7) — ждёт агента fixtures, потому что все
проверки 3.x привязаны к реальным таймкодам эталонной записи, которые тот ищет.

Волна 3: сквозная проверка 7.1–7.6 — после всех остальных.

### Решение 1.1 — почему связь между стадиями только через `contracts.py`

Пакеты стадий не импортируют друг друга. Это позволило запустить 7 агентов
параллельно без конфликтов: каждый видит только свои требования и общий тип.
Оркестрация (порядок D2, выгрузка моделей D7) — целиком в `cli.py`, стадии
о ней не знают.

### Решение 1.2 — тяжёлые модели здесь не ставятся

PaddleOCR, pix2tex, Qwen2.5-VL, GigaAM-v2, Whisper large-v3, silero-vad —
гигабайты весов и GPU-колёса, на macOS без CUDA бесполезны. Всем агентам задано:
ленивый импорт внутри `check_availability()`, честный `Availability(False, reason)`
без установленной библиотеки, а вся логика вокруг моделей (реестры, роутинг,
сборка, глоссарий, выравнивание пунктуации, деградация без глоссария) — в чистых
функциях, покрытых тестами на фейковых бэкендах. Тесты на реальных весах написаны
и помечены маркером `models` — запускаются на целевой машине.

Обоснование: иначе половина спеки осталась бы непокрытой вообще, а так проверено
всё, кроме собственно качества распознавания.

## 2. Волна 1 — отчёты

### 2.1 core (1.3, 1.4) — готово, 40 тестов зелёные

Реализованы `cli.py` (argparse, оркестрация цепочки D2, `unload()` между стадиями),
`config.py` + `default_config.yaml`, `cache.py`, `logging_setup.py`.

Ключевая проверка 1.4 — `test_param_change_invalidates_own_stage_and_later_only`,
параметризована по всем 8 стадиям: смена параметра инвалидирует свою стадию и все
последующие, предыдущие остаются хитами.

**Решение 2.1.1 (принял агент, оставляю).** Цепочка ключей кэша линейна:
ключ стадии зависит от ключа предыдущей. Следствие — консервативная инвалидация
(смена `frames_fps` инвалидирует и ASR). Это ровно то, что требует 1.4; более
узкий граф зависимостей — возможное будущее уточнение, в скоуп не входит.

**Решение 2.1.2 (принял агент).** Идентичность входного файла по умолчанию —
`stat` (путь+размер+mtime); для 830 МБ есть режим `sample` (размер + 3 чанка по 1 МиБ)
и `content` (потоковый sha256). Чтение 830 МБ на каждом прогоне недопустимо.

**Решение 2.1.3 (принял агент).** `--no-cache` = «не читать кэш», запись артефактов
сохраняется — иначе стадиям некуда писать.

**Решение 2.1.4 (главная сессия).** В `pyproject.toml` добавлен
`[tool.setuptools.package-data] lecture_transcript = ["*.yaml"]` — без этого
`default_config.yaml` не попал бы в собранный wheel (в editable работало и ошибка
осталась бы незамеченной до сборки образа).

**Открытый стык (решается в конце волны).** core ввёл соглашение о вызове стадий:
пакет стадии экспортирует `run_stage(ctx)` с `cli.StageContext`. Агенты стадий 2–6
о нём не знали (запускались одновременно). Решено НЕ прерывать их: каждый делает
естественный API своего пакета, а после завершения волны агенту core отправляется
задание дописать в `cli.py` адаптеры под фактические сигнатуры. Так дешевле, чем
переделывать 5 пакетов под соглашение, придуманное параллельно с ними.

### 2.2 media-ingest (2.1–2.4) — готово

Реализованы `probe.py`, `audio.py`, `frames.py`, `_ffmpeg.py`. Тесты зелёные,
включая прогон на эталонной записи.

Факты, добытые агентом на эталонной записи: длительность **5482.13 с** (91:22),
извлечённый WAV — 5482.112 с, расхождение 18 мс; при 1 fps получено 5482 кадра
за 4 мин 20 с на CPU.

**Решение 2.2.1 (принял агент, оставляю) — кадры в JPEG q=2, не PNG.**
5500 кадров 1080p: PNG ≈ 10 ГБ против ≈ 1.5 ГБ JPEG. Дисперсия (D3) и phash (D4)
к JPEG q=2 устойчивы. **Следствие для slide-extraction:** кроп репрезентативного
кадра для OCR нельзя резать из JPEG-выборки — его надо переизвлечь точечно из mp4
в PNG (seek на одну секунду). Это условие передано агенту волны 2.

**Решение 2.2.2 (принял агент) — таймкоды кадров детерминированно `i / fps`.**
Фильтр `fps=N` строит равномерную сетку от нуля; строгая монотонность по
построению. Реальные pts потребовали бы второго прохода и могут повторяться при
дублировании кадров.

**Решение 2.2.3 (принял агент) — явно запрошенный недоступный hwaccel падает,
не откатывается молча.** На 91-минутной записи тихий откат превращает 2 минуты
в десятки, и это обязано быть заметно. Режим `auto` откатывается на CPU штатно.

**Решение 2.2.4 (главная сессия) — допуск длительности аудио 0.25 с.**
Оставлен как предложил агент: AAC priming/padding даёт 21–43 мс, наблюдаемое
расхождение 18 мс; допуск на порядок выше наблюдаемого и на порядок ниже
интервала выборки кадров.

**Решение 2.2.5 (главная сессия) — в контракт добавлена `MissingVideoTrackError`.**
Для «нет видеодорожки» бросалась базовая `PipelineError`, что не отличало этот
случай от любого другого сбоя. Симметрично `MissingAudioTrackError`. Агент
media-ingest переключён на неё. `MediaProcessingError` и `HwaccelUnavailableError`
оставлены локальными в пакете — они специфичны для стадии.

### 2.3 speech-transcription (5.1–5.8) — готово, 24 теста зелёные, 3 под маркером `models`

Реализованы `vad.py` (silero + энергетический бэкенд), `registry.py`, `base.py`
(`IntervalAsrBackend`), `gigaam_backend.py`, `whisper_backend.py`, `punctuation.py`,
`offline.py`, `pipeline.py`.

**Решение 2.3.1 (принял агент, ключевое) — выравнивание пунктуации по «ядру».**
D6 требует, чтобы таймкоды брались из ASR и не пересчитывались. Реализовано так:
поток `isalnum()`-символов исходных слов сравнивается с потоком пунктуированного
текста. Расхождение → пунктуация **не применяется** вовсе, выдаётся warning,
слова остаются исходными. Совпадение → каждое слово забирает свои символы плюс
примыкающие знаки. Состав слов и `start_s`/`end_s` сохраняются по построению.
Это сильнее, чем «постараться сохранить»: модель физически не может сдвинуть
таймкод, потому что таймкоды вообще не проходят через неё.

**Решение 2.3.2 (принял агент) — энергетический VAD как явный бэкенд, не фолбэк.**
`silero` / `energy` / `auto`; в `auto` при отсутствии silero — переход на energy
с предупреждением в лог и в `VadResult.warnings`, не молча. Это дало возможность
реально проверить 5.2 (абсолютность таймкодов после длинной паузы) здесь, без весов.

**Решение 2.3.3 (принял агент) — GigaAM не даёт по-словных таймкодов**,
слова распределяются внутри интервала VAD пропорционально длине. Точность привязки
ограничена длиной интервала VAD. Задокументировано в docstring; на качество
секционирования (6.1/6.2) влияет слабо, потому что границы секций — это слайды.

**Решение 2.3.4 (главная сессия) — тяжёлые runtime-зависимости НЕ дублируются
в `pyproject.toml` как extras.** Агент предложил `[project.optional-dependencies] asr`.
Отклонено: единственный источник правды по боевым версиям — `docker/requirements.txt`
(задача 1.2), где версии пинуются под CUDA-образ. Дублирование списка в двух местах
гарантированно разъедется. `pyproject.toml` остаётся описанием dev-окружения.

**Не проверено здесь:** 5.3, 5.4 (нужны веса GigaAM-v2 и Whisper large-v3),
реальный silero-vad, реальная модель пунктуации, прогон 5.8 в контейнере без сети
(заменён патчем `socket`/`getaddrinfo` + офлайн-переменными окружения).

### 2.4 transcript-assembly (6.1–6.6) — готово, 31 тест зелёный

Реализованы `merge.py`, `cleanup.py`, `paragraphs.py`, `render.py`, `pipeline.py`.

**Решение 2.4.1 (принял агент) — правило допуска на границах секций (6.2).**
Разрез между соседними секциями сдвигается к ближайшей границе фразы (промежуток
между словами ≥ 0.45 с либо конец предложения), но не более чем на
`section_boundary_tolerance_s` (дефолт 2.0 с, плейсхолдер под 7.6). Сдвиг не может
опустошить ни одну из двух соседних секций — это и есть защита от «съедания»
короткого слайда. Кандидата в окне нет → разрез остаётся на месте.

**Решение 2.4.2 (принял агент) — чистка филлеров только фильтрует список слов,
`Word` не пересоздаётся.** Тождество объектов проверяется тестом. Следствие:
таймкоды физически не могут сдвинуться при чистке. Порядок стадий зафиксирован:
merge (полнота 6.1) → cleanup → paragraphs → render — полнота проверяется ДО чистки,
иначе инвариант «ни одно слово не потеряно» противоречил бы удалению филлеров.

**Решение 2.4.3 (принял агент) — абзац не начинается от одной паузы.**
Разрыв ставится, только когда абзац «созрел» (≥45 с или ≥90 слов) И есть смысловая
граница; принудительный разрыв при 180 с / 320 слов. Это прямо отвечает требованию
6.4 «не одним блоком и не по каждой паузе».

**Решение 2.4.4 (принял агент) — ссылку на PNG рендерит `render.py`,
а картинки из `SlideOcr.markdown` вырезаются.** Иначе ссылка задваивалась бы:
её обязан класть и slide-ocr (4.5), и шаблон (6.5). Путь считается через
`os.path.relpath` относительно каталога транскрипта — транскрипт переносим.

**Проверено на синтетике, не на эталонной записи:** 6.2 (построен эквивалентный
момент переключения слайда) и 6.4 (синтетический десятиминутный монолог, 840 слов,
~70 пауз). Причина: стадий выше по пайплайну на момент работы агента ещё не было.
Реальная проверка — в сквозной задаче 7.1.

**Замечание в очередь на 7.6:** `phrase_gap_s` и параметры абзацев пока живут
дефолтами dataclass-политик модулей, а не в общем конфиге. При подборе значений
их надо поднять в `TranscriptAssemblyConfig`.

**Замечание в очередь на интеграцию:** в полном прогоне `pytest` падает по таймауту
`test_extract_frames_reference_1fps` (4 мин 20 с на эталонной записи). Нужен
`--timeout` либо исключение `slow` из дефолтного прогона.

### 2.5 slide-ocr (4.1–4.9) — готово, 66 тестов зелёные, 2 под маркером `models`

Реализованы `registry.py`, `routing.py`, `paddle_backend.py`, `pix2tex_backend.py`,
`hybrid_backend.py`, `vlm_backend.py`, `assemble.py`, `glossary.py`, `offline.py`.

> ### ⚠ Расхождение спеки с реальностью — решено самостоятельно
>
> **Слайда «Пример 2. √(x²−x) = −5» в эталонной записи НЕТ.** Это иллюстрация
> из `design.md` (D5), а не кадр записи. На неё ссылаются формулировки проверок
> в задачах **3.5** и **4.4**.
>
> Решение: считать её иллюстративной и проверять на реальном ближайшем аналоге —
> печатном слайде **«Пример 4. √(12−x) = x»** на t≈4260 с. Таймкод найден
> просмотром кадров эталона, задокументирован в самом тесте.
>
> Альтернатива — объявить 3.5 и 4.4 невыполнимыми — отвергнута: проверяемое
> свойство (репрезентативный кадр содержит дописанное решение; печатная формула
> распознаётся в LaTeX) от номера слайда не зависит.
>
> Второй найденный таймкод: **t≈425 с** — слайд «Как называются эти числа
> 1, 2, 3, 4, 5, 6, …?» из задачи 4.2, этот существует как заявлено.

**Решение 2.5.1 (принял агент) — отсечение оформления в глоссарии по геометрии,
без стоп-листов.** Надпись считается элементом оформления, если встречается
на ≥80% слайдов (минимум на 3) И центры её боксов расходятся ≤32 px. Это прямо
отвечает требованию design «никаких хардкодов под оформление конкретного шаблона»:
логотип платформы отсекается тем, что он всегда на одном месте, а не тем, что
он внесён в список.

**Решение 2.5.2 (принял агент) — правило `unreliable`.** Фрагмент неуверенный при
`confidence < 0.60`; слайд ненадёжен, если фрагменты есть и доля неуверенных ≥ 0.5.
Пустой слайд ненадёжным не считается — у него просто нет содержимого, и ссылка
на PNG всё равно есть.

**Решение 2.5.3 (принял агент) — формульному фрагменту ставится
`min(0.85, confidence исходной строки)`.** pix2tex своей уверенности не отдаёт;
потолок 0.85 отражает заявленную в D5 точность ~85%, а протаскивание confidence
исходной строки не даёт размытой строке стать «уверенной формулой».

**Не проверено здесь:** 4.2 и 4.4 на реальных весах (PaddleOCR, pix2tex не ставились),
реальное поведение `HybridBackend` на настоящих боксах, прогон 4.9 в контейнере
без сети (эмулирован патчем сокетов).

### 2.6 infra (1.1, 1.2) — артефакты готовы, проверка требует целевой машины

Созданы `docker/Dockerfile`, `compose.yaml`, `verify_env.sh`, четыре
`requirements*.txt`, `README.md`. Статические проверки — `tests/test_docker_artifacts.py`.

**Факт, добытый экспериментом (а не предположением):** apt-овый ffmpeg в
Ubuntu 22.04 **уже умеет nvdec**. Агент поднял `ubuntu:22.04` под amd64-эмуляцией,
поставил ffmpeg из apt: `-hwaccels` даёт `cuda`, `-decoders` — `h264_cuvid`.
Сборка ffmpeg из исходников не нужна. Флагов `--enable-nvdec` в строке configure
нет потому, что ffmpeg включает их автоматически при наличии заголовков ffnvcodec.

**Решение 2.6.1 (принял агент, неочевидное и важное) —
`NVIDIA_DRIVER_CAPABILITIES=compute,utility,video`.** По умолчанию
nvidia-container-toolkit прокидывает только `compute,utility`, и `libnvcuvid.so.1`
в контейнер не попадает. Симптом коварный: `nvidia-smi` работает, `-hwaccels`
показывает `cuda`, а декодирование падает. Прописано и в Dockerfile, и в compose;
в `verify_env.sh` есть отдельная проверка на `libnvcuvid` и сквозной тест
«закодировать libx264 → декодировать через `-hwaccel cuda -c:v h264_cuvid`».

**Решение 2.6.2 (принял агент) — весь стек версий продиктован `gigaam==0.1.0`,**
который объявляет `torch<=2.5.1`. Отсюда torch 2.5.1+cu124, torchvision 0.20.1,
и всё остальное выровнено на релизы того же периода. Не «взял свежее», а
«взял то, что совместимо с самым жёстко ограниченным пакетом».

**Решение 2.6.3 (принял агент) — `ctranslate2==4.6.3` требует cuDNN 9,**
поэтому база образа с тегом `-cudnn-` (cuDNN 9), а не cuDNN 8.

**Решение 2.6.4 (принял агент) — конфликт `cv2`.** pix2tex/albumentations тянут
`opencv-python-headless`, paddlex пинит `opencv-contrib-python==4.10.0.84`.
Обе выровнены на 4.10.0.84, contrib переустанавливается последним шагом сборки,
чтобы победитель был детерминированным.

**Риск, зафиксированный честно:** индекс `paddlepaddle.org.cn` с dev-машины
недоступен, существование `paddlepaddle-gpu==3.2.0` подтвердить не удалось.
Основание косвенное — CPU-двойник `paddlepaddle==3.2.0` на PyPI есть с колесом
cp311. Это самый рискованный пин в наборе, в `docker/README.md` описано,
что делать, если он не встанет.

**Проверено здесь:** `docker buildx build --check` без предупреждений, тег базового
образа резолвится в registry, `docker compose config` OK, `bash -n verify_env.sh` OK,
`uv pip compile --python-platform x86_64-unknown-linux-gnu` разрешает весь набор
без конфликтов.

**Не проверено (нужна RTX 4060):** сборка образа, `nvidia-smi`, реальный nvdec,
`torch.cuda.is_available()`, установка GPU-колёс. Всё закрывается одним запуском
`docker/verify_env.sh` на целевой машине. Поэтому **1.1 и 1.2 не отмечаются
выполненными** — артефакт есть, проверка из формулировки задачи не выполнена.

### 2.7 fixtures (1.5) — готово, 17 тестов зелёные

Созданы `tools/make_fixtures.py`, `tests/conftest.py`, `tests/fixtures_manifest.json`,
пять клипов в `tests/fixtures/` (26 МБ, gitignored).

**Метод разведки:** вся запись развёрнута в 5482 кадра 1 fps шириной 480; по ним —
доля пикселей ярче 150 (детект «демонстрация включена»), медиана разности соседних
кадров и медианная яркость в скользящем окне (область по D3), phash кропа
(hash_size=16). Каждый выбранный интервал просмотрен глазами по монтажам кадров.

**Добытые факты по эталонной записи** (опорные для всех задач 3.x):

| Что | Значение |
|---|---|
| Длительность | 5482.13 с, h264 1920x1080, 25 fps, аудио aac моно 48 кГц |
| Область демонстрации | `x=64, y=60, w=1248, h=656` (светлое поле слайда без рамки шаблона) |
| Демонстрация включается | t=60 с |
| Интервалов без демонстрации | 33, суммарно 247 с; кроме начального — все 3–13 с |
| Проверка таймкодов из 3.2 | t=600 слайд есть, t=2400 нет (2399–2412), t=4200 есть — **подтвердилось** |
| Смен слайда при hamming≥40 | 64 события, 54 группы длиннее 4 с |
| Интервалов дописывания маркером | 15; самый длинный 2468–3017 («Метод замены переменной», 107 шагов) |

> ### ⚠ Находка, меняющая алгоритм 3.3
>
> **Переключение слайда на этой платформе сопровождается кратким (~4 с)
> исчезновением демонстрации.** Без гистерезиса каждая смена слайда была бы
> засчитана как «демонстрация выключена» — 64 ложных интервала вместо 33 реальных.
> Требование гистерезиса передано агенту slide-extraction явным пунктом.

**Расхождение с design.md, зафиксированное честно:** design говорит «демонстрация
включается и выключается по ходу пары», подразумевая длительные интервалы.
Фактически единственный отрезок без демонстрации длиннее 30 с — это первая минута
записи. Остальные выключения длятся 3–13 с. `clip_no_slide` пришлось взять из
начала записи, где речи мало (34%).

**Решение 2.7.1 (принял агент) — нарезка клипов с перекодированием (libx264 crf 23),
а не `-c copy`.** Нужны точные границы и keyframe в начале клипа; при `-c copy`
границы съезжают до ближайшего keyframe, и «момент переключения слайда»
мог бы не попасть внутрь клипа.

**Решение 2.7.2 (принял агент) — фикстуры не автогенерируются молча.**
Нет клипов → `pytest.skip` с подсказкой запустить `tools/make_fixtures.py`;
автонарезка только по опт-ину `LECTURE_FIXTURES_AUTOGEN=1`. Иначе первый прогон
тестов на чужой машине молча резал бы 830 МБ.

---

## 3. Волна 2 и аудит

Запущен агент slide-extraction (3.1–3.7) с полным набором добытых таймкодов,
предупреждением о гистерезисе и о том, что слайда «Пример 2» в записи нет.

Параллельно, не дожидаясь его, запущены аудиторы по уже готовым возможностям —
время сабагентов дешевле, чем последовательное ожидание.

Полный прогон репозитория на этот момент: **213 passed, 6 skipped, exit 0**.
Отмечено выполненными 21 из 45 задач.

### Решение 3.1 — обрыв по лимиту API и рестарт

Первый запуск волны 2 (slide-extraction + 5 аудиторов, 6 агентов Opus
одновременно) целиком лёг на лимите сессии. Ни один агент не успел записать код.

Решение: **не перезапускать всё разом.** Агенты возобновляются через
`SendMessage` (сохраняют исходное задание, не требуют пересылки промпта) и
партиями по 2–3, а не по 6. Первая партия: slide-extraction + аудиты
media-ingest и transcript-assembly. Остальные аудиты — следующей партией.


## 4. Аудит и правки

### 4.1 Аудит media-ingest — 1×P0, 6×P1, 4×P2

> **P0: требование спеки «Переиспользование промежуточных артефактов»
> не реализовано.** `extract_audio`/`extract_frames` всегда декодируют заново,
> точки входа стадии нет, к `cache.py` стадия не подключена.
>
> Это прямое следствие открытого стыка из решения 2.1: core придумал соглашение
> `run_stage(ctx)` уже после того, как агенты стадий стартовали, и никто из них
> о нём не знал. Аудит поймал это как дефект — правильно поймал.

Прочие подтверждённые находки P1 (все воспроизведены аудитором):

1. Смещение аудиодорожки (`start_time` ≠ 0) теряется — WAV молча сдвинут
   и короче записи (на синтетике: 10.03 с вместо 13.0 с, тон уехал на −3 с).
2. `%` в пути выходного каталога ломает извлечение кадров — ffmpeg `image2`
   разворачивает printf-шаблон по всему пути.
3. При отсутствии видео пользователь не предупреждён о неполноте результата
   (спека этого требует), есть только INFO в лог.
4. Тест точности временны́х меток сверяет метки с той же формулой `i/fps`,
   что и реализация — упасть не может. Сама реализация верна, проверено отдельно.
5. `test_resolve_hwaccel_none_and_auto` тавтологичен.
6. `atomic_dir` оставляет вечный `.tmp-….old`, если целевой путь занят файлом.

Дефектами НЕ являются (проверено аудитором и отклонено): контракты, кросс-импорты,
разбор ffprobe с `N/A`/`0/0`/дробями, пути с пробелами/юникодом/`:`, обработка
ненулевого exit ffmpeg, детерминированность порядка кадров.

Запущен отдельный агент правок. Ему запрещено трогать `cli.py` — точку входа
он реализует на своей стороне, недостающую стыковку опишет в отчёте.

### 4.2 Аудит transcript-assembly — 1×P0, 6×P1, 9×P2

Аудитор применил мутационное тестирование (снимал защиту и смотрел, упадут ли
тесты) и фаззинг на 14 000 входов. Инвариант полноты 6.1 сломать не удалось —
он держится. Кросс-процессный тест детерминированности настоящий.

> **P0: `merge.py` сортирует секции по индексу слова, а не по времени.**
> На валидном входе получается «Слайд 1 → Слайд 3 → Слайд 2» с обратными
> интервалами в заголовках. Воспроизведено.

> **P1, самое важное по существу задачи: чистка филлеров ломает содержание
> математической лекции.**
>
> - «два плюс два плюс два» → «два плюс два» (дедуп n-грамм рушит арифметику)
> - «ноль ноль один» → «ноль один»
> - «Пусть n n стремится» → «Пусть n стремится»
> - «Как бы вы доказали это» → «вы доказали это» (фразовый филлер режется безусловно)
> - «Это самое важное свойство» → «важное свойство»
> - «Он он знает» → «он знает» (теряется заглавная — удаляется ранняя копия)
> - «длина равна пяти м» → «...пяти» («м» в списке филлеров съедает единицу измерения)
>
> Это прямое нарушение design → Risks: «правила консервативные, чистятся только
> явные заполнители; сомнительное остаётся». Для лекции по математике цена ошибки
> здесь максимальная: испорченная формула в конспекте не отличима от верной.

Прочие P1: первый абзац секции никогда не получает таймкод (`position > 0`),
нарушая требование «Таймкоды для сверки»; тест `test_tolerance_does_not_swallow_short_slide`
не может упасть — аудитор снял всю защиту 6.2, тесты остались зелёными.

Запущен отдельный агент правок с явным требованием сделать чистку строго
консервативнее: числительные, однобуквенные математические обозначения и единицы
измерения выводятся из-под дедупа и из списка филлеров.

### 4.3 Аудит slide-ocr — 4×P0, 10×P1, 6×P2

Существующие 66 тестов не ловят ни одну находку — это само по себе диагноз
по качеству тестов стадии.

P0: `parse_paddle_output` падает `ValueError` на выдаче PaddleOCR 3.x (проверка
`or` над `numpy.ndarray`); `sort_fragments` переворачивает строку при джиттере
bbox в 2 px; отсечение оформления в глоссарии использует `all(...)` вместо доли,
поэтому один выбившийся bbox пропускает логотип платформы в глоссарий;
дефолт `backend: paddle+pix2tex` не существует в реестре (`hybrid|paddle|vlm`).

> **P1 №5 — содержательно самая важная находка по всей стадии.**
> Строка с низкой уверенностью (типично — рукописная пометка маркером) уходит
> в pix2tex и **заменяется галлюцинированным LaTeX**.
>
> Это ровно то, что design D5 запрещает буквальным текстом: «изображение слайда
> под рукой полезнее, чем галлюцинация модели на месте рукописной записи».
> Роутинг «низкая уверенность → формульный распознаватель» задумывался для
> печатных формул, которые PaddleOCR плохо читает, и был по недосмотру
> распространён на рукописное.

Парная находка 3/7: эвристика отсечения оформления одновременно пропускает
логотип платформы и отсекает настоящий термин лекции (заголовок темы в шапке
каждого слайда). Чинить надо вместе — правка одной усугубляет другую.

**Решение 4.3.1 (главная сессия).** Дефолт `slide_ocr.backend` в
`default_config.yaml` переведён с несуществующего `paddle+pix2tex` на `hybrid`.
Сделано в главной сессии, а не агентом правок: файл конфига — общий,
и в этот момент в него писал агент slide-extraction (порог 3.7).

### 4.4 Аудит speech-transcription — 4×P0, 5×P1, 5×P2

> **P0 №1 — блокирует боевой прогон.** Кусок для GigaAM = интервал VAD без
> ограничения длины, а `gigaam==0.1.0` бросает `ValueError("Too long wav file")`
> при длине >25 с. На эталонной записи VAD дал **один интервал 5459 с**.
> Дефолтная конфигурация упала бы на первой же реальной лекции.
>
> Аудитор проверил лимит по распакованному колесу пинованной версии, а не по
> документации — это и есть та проверка, которую нельзя было сделать «на глаз».

Прочие P0: пустой результат VAD трактуется как «распознать всё аудио» (10 с тишины
дали 20 слов, без предупреждения); единственный зарегистрированный пунктуатор
требует pip-пакет, которого нет в `docker/requirements.txt`, и отказ тихий —
транскрипт молча уедет без пунктуации и регистра.

P1: `test_words_after_long_pause_have_absolute_timecodes` проходит при полностью
убранном пересчёте offset (кламп подтягивает времена к началу интервала) —
контрольные мутанты набор ловит, а этот нет; `used_glossary=True` для GigaAM,
хотя параметра `hotwords` в `gigaam 0.1.0` нет вообще, из-за чего проверка 5.3
прошла бы всегда; ложная доступность silero через `find_spec("torch.hub")`;
пик памяти 1867 МБ и двойное чтение WAV на 91-минутной записи.

Оба агента правок запущены. Межмодульный пункт (пакет пунктуатора для образа)
агент опишет в отчёте — передам агенту окружения отдельно, чужие файлы он не трогает.

### 4.5 Правки transcript-assembly — закрыто 7 находок P0/P1 + 3×P2, тестов 31 → 51

**Решение 4.5.1 (принял агент правок) — новое правило чистки филлеров.**
Удаление разрешено только при отсутствии сомнения:

- числительные, однобуквенные токены и слова формул выведены из-под дедупа
  целиком — в математической лекции их повтор нормален;
- список фразовых филлеров разделён надвое: однозначные удаляются всегда,
  омонимичные («как бы», «это самое», «так сказать») — только там, где оборот
  не открывает предложение И обособлен паузой/запятой **с обеих** сторон;
- при повторе с заглавной буквы удаляется поздняя копия, а не ранняя.

Каждое правило только СУЖАЕТ удаление — ни один ранее сохранявшийся фрагмент
теперь не режется. Осознанная цена: часть филлеров остаётся («предел как бы
равен» не чистится), и это зафиксировано тестом как ожидаемое поведение.
Обоснование — design → Risks: «сомнительное остаётся».

**Решение 4.5.2 (принял агент) — P0 с порядком секций починен конструктивно.**
Время начала run'ов монотонизируется, поэтому стабильная сортировка не может
переставить их между собой: инвариант полноты 6.1 защищён не проверкой, а
структурой. Фаззер аудитора: нарушений порядка было 2 → стало 0; на невалидных
пересекающихся входах 2233 → 168.

**Отклонено агентом с обоснованием (согласен, не настаиваю):** экранирование
Markdown в тексте речи (меняет текст, риск выше пользы), `Path.resolve()`
в relpath (ходит в ФС и разыменовывает симлинки — против побайтовой
детерминированности 6.6), `raise` при пересекающихся интервалах слайдов
(меняет контракт публичной функции; такой вход и так отбрасывается валидатором).

**Проверка «падал бы до правки» выполнена буквально:** все 19 новых и
переписанных проверок прогнаны против откаченных исходников — 19 failed,
после восстановления зелено.

### 4.6 Правки media-ingest — закрыты все P0/P1, тестов стало 43

**P0 закрыт:** появился `media_ingest/stage.py` с точкой входа стадии по
соглашению core. Проверка не «файл на месте», а по существу: счётчик запусков
ffmpeg/ffprobe плюс mtime и размеры кадров — повторный прогон **не декодирует**.
Есть и обратный тест: `--no-cache` заставляет декодировать.

**Решение 4.6.1 (принял агент) — кадры в `meta.json` не перечисляются.**
5500 имён в метаданных кэша — это мусор; список восстанавливается
`sorted(glob)` по каталогу. Пути в нагрузке относительные, чтобы кэш
не ломался при переносе каталога.

**Решение 4.6.2 (принял агент) — `-af aresample=async=1:first_pts=0`.**
Так лечится потеря смещения аудиодорожки (`start_time` ≠ 0). До правки
тон уезжал с 8-й секунды на 5-ю, длительность 10.03 вместо 13 с.
Побочный полезный эффект: аудио и кадры теперь заведомо в одной системе отсчёта.

**Решение 4.6.3 (принял агент) — ffmpeg запускается с `cwd=tmp_dir`,**
шаблон кадров относительный. Так `%` в пути выходного каталога перестаёт
разворачиваться как printf-шаблон.

**Переписан тест точности меток (была тавтология).** Теперь клип кодирует
секунду геометрией кадра — число белых колонок равно `floor(T)+1`, устойчиво
к limited/full range и потерям кодека. Проверено буквально: подмена формулы
на `i/fps + 5` роняет тест.

**Отклонено агентом с обоснованием (согласен):** доводить смещение кадров
на 50 мс (сам аудит признал, что укладывается в требуемую точность);
вместо кода задокументирована нулевая точка отсчёта.

**Открытые пункты, переданные агенту core** (чужая зона, агент правок
её не трогал — это правильно):

1. Стадиям недоступен каталог ячейки кэша, поэтому относительные пути
   из нагрузки не во что разворачивать после попадания в кэш.
2. `config.stage_params("frames")` берёт всю секцию `media_ingest`, поэтому
   смена `audio_track` инвалидирует и кадры — лишнее переизвлечение 5500 кадров.
3. Чужой тест `test_run_without_implemented_stages_does_not_crash` начал падать
   закономерно: он ждал `code == 0` от фейкового mp4, а теперь стадия `frames`
   реализована и `probe` корректно отвергает не-медиа. Чинить надо тест,
   а не поведение.


## 5. slide-extraction (3.1–3.7) — готово, 36 тестов

> ### ⚠ ПОПРАВКА к разделу 2.5 — я ошибся, и это надо знать
>
> В разделе 2.5 записано, что слайда «Пример 2. √(x²−x) = −5» в эталонной записи
> нет и что это иллюстрация из design.md. **Это неверно.** Агент slide-extraction
> прошёл всю запись покадрово и нашёл его: **t≈4198–4225 с, группа #35**.
>
> Ошибка родилась так: агент slide-ocr искал слайд по кадрам выборочно, не нашёл
> и сделал вывод об отсутствии; я принял этот вывод и разослал его дальше как
> факт — в том числе в задание агенту slide-extraction. Правильная реакция была бы
> «не нашли» ≠ «нет».
>
> Последствие: задачи **3.5 и 4.4 проверяются ровно на том слайде, который
> назван в их формулировке**, без замены на аналог. Агенту slide-ocr отправлена
> поправка с точным таймкодом.

### Результат 3.7 — порог hamming

**`hamming_threshold: 44`**, hash_size=16. Расстояния на эталоне разошлись
с зазором: дописывание маркером и сдвиги окна демонстрации дают максимум **42**,
самая слабая настоящая смена слайда — **46**. Любой порог 43…46 даёт одни и те же
**49 групп**; взята середина зазора, ближе к нижнему краю — дробление безопаснее
склейки (design D4/Risks).

Ручной подсчёт, которого требует задача, выполнен: 6 монтажей всех 49
репрезентативных кадров плюс 9 пар «до/после» для всех пограничных скачков.
Итог: **43 логических слайда**, 6 лишних делений (окно демонстрации сдвигалось
по вертикали без смены содержимого; phash при этом прыгает на 106–116, порогом
не лечится), **склеек двух разных слайдов нет ни одной**.

### Решения, принятые агентом (оставляю все)

**5.1 — разброс считается робастно (MAD×1.4826), а не как обычная σ.**
Четыре кадра «провала демонстрации» из 300 поднимают σ слайда до ~20 и рушат
маску. Это ровно тот случай, где обычная дисперсия из D3 не работает на реальных
данных, а идея D3 — работает.

**5.2 — гистерезис `min_absence_s = 20 с` и порядок операций.** Все 44 артефакта
переключения ≤13 с, единственное настоящее выключение — 60 с. Сначала гасятся
всплески в 1–2 кадра, потом заполняются короткие провалы; обратный порядок
«лечил» настоящее выключение. Результат на эталоне: ровно 2 интервала раскладки
вместо 45 сырых провалов.

**5.3 — область интервала = объединение областей его окон** (`stable_region_iou=0.75`).
Шаблон слайдов оставляет светлым разное поле, и по одному окну получалось бы
пересечение — кроп для OCR терял бы содержимое справа.

**5.4 — PNG-кроп переизвлекается точечно из mp4** (`ffmpeg -ss T -frames:v 1 -vf crop`),
с откатом на кроп из JPEG-выборки и предупреждением в лог. Это закрывает
следствие решения 2.2.1 (кадры выборки — JPEG q=2, для OCR формул не годятся).

### Открытые пункты, переданные агенту core

- `SlideExtractionConfig.hamming_threshold` в dataclass остался `8`, тогда как
  в YAML стоит подобранное `44`. YAML при `load_config` побеждает, но любой код,
  создающий конфиг напрямую, получит заведомо неверное значение. Это уже вторая
  найденная рассинхронизация dataclass↔YAML (первая — дефолт OCR-бэкенда),
  поэтому core поручено закрыть её автоматической сверкой в тесте.
- Параметры `min_absence_s`, `hash_size`, `max_spread`, `min_brightness`,
  `stable_region_iou` живут константами модулей и в конфиг не подняты.

Отмечено выполненными 28 из 45 задач. Запущен аудитор slide-extraction
с заданием перепроверить числа порога самостоятельно, а не поверить на слово.

### 5.1 Правки slide-ocr — закрыты все P0 и P1, тестов 66 → 98

**Решение 5.1.1 (принял агент правок) — правило «рукописное против печатной
формулы», трёхчастное.** Это ответ на самую важную находку аудита:

- низкая уверенность уводит строку в pix2tex **только при наличии
  математического признака** — она снимает порог доли слов, но не заменяет
  собой наличие математики;
- строка с уверенностью < 0.35 и вовсе без математики опознаётся как рукописная
  и **выбрасывается гибридом целиком** — ни текстом, ни формулой; доступ к ней
  даёт ссылка на PNG. Это буквальная реализация D5, категория [3];
- если строка ушла в pix2tex только по уверенности, результат помечается
  `low_confidence` («сверьтесь с изображением»), а не выдаёт себя за надёжный.

Страховка от перегиба тоже покрыта тестом: плохо прочитанная **печатная**
формула в pix2tex всё ещё уходит.

**Решение 5.1.2 (принял агент) — правило отсечения оформления стало
трёхчастным и наконец различает два похожих случая.** Частота ≥0.8 слайдов
И ≥0.8 боксов в допуске от медианы (вместо `all`) И строка не состоит целиком
из слов, встречающихся на слайдах вне неё.

Третий пункт — суть находки: плашка платформы («Онлайн-школа») больше нигде
в лекции не звучит, а заголовок темы («Квадратные уравнения») состоит из слов
словаря самой лекции. Геометрии для их различения не хватало.

**Решение 5.1.3 (принял агент) — экранирование Markdown только для `kind="text"`,**
формулы не трогаются; путь к PNG кодируется процентами, а не оборачивается
в `<...>` — так остаётся рабочим регексп `_IMAGE_LINK` в transcript_assembly,
который вырезает картинки из врезки (решение 2.4.4).

**Решение 5.1.4 (принял агент) — `check_availability()` проверяет наличие
локальных весов без сети** (`~/.paddleocr`, `~/.paddlex`, чекпойнты пакета
pix2tex, HF-кэш) и меряет **свободную** VRAM через `mem_get_info`, а не
`total_memory`. Иначе прогон падал бы в середине при `HF_HUB_OFFLINE=1`
или ловил OOM на середине стадии.

**Проверка контракта стала содержательной.** Вместо `isinstance(..., OcrBackend)`,
которое проходило для класса с любыми сигнатурами, — сверка сигнатур через
`inspect.signature`, типа `name`, возврата `Availability` и поведения `recognize`.
В тесте есть контрпримеры, на которых старая проверка проходила, а новая ловит.

**Проверка «падал бы до правки»:** с временно откаченными правками 23 новых
теста падают, старые нет.

**Полный прогон проекта после правок: 329 passed, 28 deselected.**

### 5.2 Доработка core — стыковка стадий, дефолты, стадия `frames` подключена

**Решение 5.2.1 (принял агент core, важное и правильное) — `audio_track`
и `audio_sample_rate` НЕ убраны из ключа стадии `frames`.**

Я поручил сузить ключ, чтобы смена аудиодорожки не инвалидировала 5500 кадров.
Агент отказался и объяснил почему: `audio.wav` лежит в той же ячейке, что и кадры,
поэтому исключение этих полей дало бы не экономию, а **попадание со старым WAV** —
тихую порчу вместо trade-off. Правильное решение — разделить артефакты по разным
стадиям, а не ослабить ключ.

Это тот случай, когда исполнитель отверг указание координатора по существу
и оказался прав. Задание на второй вход `run_audio_stage` отправлено агенту
media-ingest; core добавит стадию `audio` в `STAGES`, когда вход появится.

**Решение 5.2.2 (принял агент) — `StageContext.stage_dirs: dict[str, Path]`.**
Стадия получает базу, относительно которой разворачивает нагрузку предыдущей:
для попадания в кэш это `cell.path`, для считающейся стадии — её `build_dir`,
после фиксации перезаписывается на каталог ячейки.

**Рассинхронизация дефолтов закрыта автоматической сверкой.** Найдено ровно две
(`hamming_threshold` 8→44, `slide_ocr.backend`), обе исправлены; добавлен
`test_dataclass_defaults_match_yaml`, который сверяет ВСЕ поля в обе стороны
и печатает конкретный дифф. Больше эта категория ошибок не повторится молча.

**Решение 5.2.3 (принял агент) — какие константы поднимать в конфиг.**
Подняты `hash_size`, `min_absence_s`, `max_spread`, `min_brightness`.
Критерий внятный: `hash_size` неотделим от порога (порог измеряется в битах
именно этого хэша — держать их в разных местах значит разъезжаться);
`max_spread`/`min_brightness` — те самые пороги детекта области из риска D3
«тёмные слайды / другой шаблон»; `min_absence_s` зависит от манеры лектора.
`stable_region_iou` не поднят: внутренняя эвристика, менять её без чтения кода
бессмысленно. Мёртвый `audio_channels` удалён совсем — его никто не читал,
моно зафиксировано контрактом.

**Открытый пункт для slide-extraction:** блок `if config is not None` в
`extract_slides` читает только 5 полей, четыре новых пока не подхватываются —
это мёртвые ручки. Передам вместе с правками по аудиту, чтобы не править код,
который сейчас читает аудитор.

Полный прогон после доработки: **330 passed, 3 skipped**.

### 5.3 Правки speech-transcription — закрыты все P0/P1, тестов 24 → 53

**Решение 5.3.1 (принял агент) — нарезка длинных интервалов VAD под лимит модели.**
`GigaAmBackend.max_chunk_s = 20.0` с запасом к порогу 25 с из `gigaam 0.1.0`.
Разрез ищется по минимуму энергии в хвостовом окне 0.75 с, то есть попадает
в паузу, а не в середину слова. Живая проверка на 91-минутном WAV: один интервал
VAD 5460 с → 279 кусков, максимум 19.9 с, встык без дыр.

**Решение 5.3.2 (принял агент, с обоснованием) — пустой VAD больше не значит
«распознать всё».** Введено различие: `None` = интервалы не заданы → распознаём
всё аудио; `()` = VAD речи не нашёл → пустая транскрипция и warning, модель даже
не грузится. Фолбэк на всё аудио отвергнут по двум причинам: он прямо нарушает
сценарий спеки «Длительная тишина» и кормит Whisper тишиной, то есть провоцирует
галлюцинации. Было: 10 с тишины → 20 слов. Стало: 0 слов + предупреждение.

**Решение 5.3.3 (принял агент) — память.** Одноэлементный кэш прочитанного WAV,
деление на месте вместо копии, `einsum` вместо `astype(float64)+square`,
явное освобождение после ASR. **Пик на 91-минутной записи: 1867 → 560 МБ,
файл читается один раз вместо двух.**

**Решение 5.3.4 (принял агент) — `used_glossary` считается по факту.**
GigaAM снимает флаг, когда в установленной версии нет `hotwords`; Whisper —
когда промпт пуст. До правки флаг стоял всегда, и проверка 5.3 прошла бы
независимо от того, работает глоссарий или нет.

> ### ⚠ Находка, требующая решения по образу: пунктуатора нет в дистрибутиве
>
> Единственный зарегистрированный пунктуатор — `sbert_punc_case_ru` версии 0.2.
> **На PyPI его НЕТ** (`pip install` → 404), репозиторий `github.com/kontur-ai/…`
> тоже отдаёт 404. Код лежит внутри репозитория модели на HuggingFace.
>
> Рабочая строка для образа (агент проверил `setup.py` внутри репозитория:
> name `sbert_punc_case_ru`, version 0.2, requires `transformers>=4.36.2` —
> наш пин `transformers==4.46.3` подходит):
>
> ```
> sbert_punc_case_ru @ git+https://huggingface.co/kontur-ai/sbert_punc_case_ru@f778dc6c63bb0ec235a220488862509810e54583
> ```
>
> Ставить с `GIT_LFS_SKIP_SMUDGE=1`, иначе pip затянет в билд 1.7 ГБ весов
> (веса нужны в рантайме, из `/cache`).
>
> Альтернатива без нового пакета — реализовать пунктуатор поверх `transformers`
> напрямую (модель обычная token-classification). Дороже, но не зависит от
> git-URL, который может исчезнуть так же, как исчез GitHub-репозиторий.
>
> **Решение: добавляю пин с фиксированным коммитом** — он воспроизводим и
> работает сегодня; альтернатива задокументирована как запасной путь.
> Правка уходит агенту окружения ПОСЛЕ того, как аудитор закончит читать
> `docker/` — сейчас он там работает, и одновременная правка сломала бы аудит.

**Отклонено агентом с обоснованием (согласен):** находка про расхождение
нормализации — расхождения нет, обе функции ведут себя одинаково, симптом
реален, но причина другая и поведение штатное.

**Проверка «падал бы до правки»:** прогнано 12 мутантов, откатывающих каждую
правку по отдельности — все ловятся.

### 5.4 Аудит core + docker — 0×P0, 8×P1, 11×P2

Подтверждено рабочим (аудитор проверял, а не читал): цепочка инвалидации D8,
атомарность и гонка кэша, `NVIDIA_DRIVER_CAPABILITIES=…,video` в обоих местах,
`buildx --check`/`compose config`/`bash -n` чистые, `uv pip compile` резолвит
99 пакетов без конфликтов, все пины существуют на PyPI.

Каркас (A):

1. `_validate_backend` валидирует по `available_backend_names()` вместо
   `list_backend_names()` — опечатка в имени бэкенда **не отвергается**, пайплайн
   стартует. Это нарушает задачи 4.1/5.1 «недоступный бэкенд отвергается до начала
   прогона»: отвергается не тот класс ошибок.
2. Та же подмена в `--help`: имена бэкендов не печатаются никогда, вместо них
   ложное «реестр ещё не доступен». То есть проверка задачи 1.3 считалась
   пройденной, а печати не было.
3. Прогон, не собравший `transcript.md`, возвращает **код 0**.
4. `unload()` не вызывается после `vad` и `punctuation` (D7 требует выгрузку
   между всеми модельными стадиями; у пунктуатора `unload()` есть, вызывающих нет).
5. Связка «поле конфига → стадия» не покрыта: удаление `confidence_threshold`
   из `stage_params("ocr")` переживает весь набор тестов.

> **P2, который я поднял до обязательного к правке:** `default=str` при
> сериализации ключа кэша даёт **недетерминированный ключ** для `set` и объектов —
> аудитор получил 6 разных ключей в 6 процессах. Для кэша, который должен
> переживать перезапуск, это дефект, а не косметика.

Окружение (B):

> **B2 — VLM-бэкенд в образе неработоспособен.** `transformers==4.46.3`
> не содержит `qwen2_5_vl` (404 на v4.46.3, 200 на v4.49.0), плюс отсутствуют
> `bitsandbytes` и `accelerate`, без которых 4-битная загрузка невозможна.
> Задача 4.8 требует рабочего альтернативного бэкенда, а он бы просто не запустился.
>
> Ограничение, которое нельзя снимать: `gigaam` жёстко держит `torch<=2.5.1`.
> Агенту окружения поручено найти версию `transformers` с `qwen2_5_vl`,
> совместимую с этим торчом, и **честно сказать, если такой нет** — тогда 4.8
> останется опцией, недоступной в этом образе.

B3: `test_dockerfile_pins_base_image_and_nvdec_env` не может упасть — перестановка
двух `RUN pip install` местами оставляет тесты зелёными.

Также поручено: WARN → FAIL в сквозной проверке nvdec (это ровно та проверка,
ради которой существует `verify_env.sh` и задача 1.1), `.dockerignore` при
контексте сборки 280 МБ, устранение `HF_HUB_OFFLINE: "0"` вопреки собственному
комментарию рядом.

Оба агента правок запущены по своим зонам.

### 5.5 Разделение стадий `audio` и `frames`

`media_ingest` разделён на два независимых входа: `run_audio_stage` (только
`audio.wav`) и `run_frames_stage` (только кадры). Независимость ячеек закрыта
тестами со счётчиком запусков ffmpeg: смена `audio_track` не переизвлекает кадры,
смена `frames_fps` не пересчитывает WAV.

> ### ⚠ Следствие, меняющее модель кэша: линейная цепочка перестала описывать
> реальность
>
> `cache.chain(...)` строит ключ каждой стадии поверх ключа предыдущей. Пока
> стадии шли строго в ряд, это работало. Теперь `audio` и `frames` — **две
> независимые корневые стадии**: ни одна не потребляет результат другой, обе
> зависят только от исходного файла. Если оставить их соседями в линейной цепочке,
> смена `audio_track` снова инвалидирует 5500 кадров через `previous_key` — то есть
> мы вернёмся ровно к проблеме, из-за которой стадии и разделили.
>
> Дальше развилка только усложняется: `slides` зависит только от `frames`,
> `vad` — только от `audio`, а `merge` — от обеих ветвей. Это в точности структура
> из design D2, которую линейный список стадий не выражает.
>
> Поручено core: заменить позицию в списке на явный список стадий-предшественниц
> (для `audio` и `frames` — пустой). Решение 2.1.1 («линейная цепочка, сознательно
> консервативная инвалидация») тем самым отменяется: оно было верным, пока граф
> был цепочкой, и перестало быть верным, когда граф стал деревом.

**Решение 5.5.1 (принял агент media-ingest) — `audio` идёт ПЕРЕД `frames`.**
Тогда «видео без аудиодорожки» падает сразу, а не после четырёх минут
извлечения 5500 кадров.

### 5.6 Правки docker — все находки закрыты, тестов 10 → 16

**Решение 5.6.1 — пунктуатор ставится VCS-пином с HuggingFace.** Агент проверил
сам, а не поверил на слово: HF API отдаёт 200 на `revision/f778dc6c…`, возвращённый
`sha` совпадает с пином (значит это коммит, а не ветка), в дереве есть `setup.py`
с `name="sbert_punc_case_ru"`, `version="0.2"`, `install_requires` совместим с нашим
`transformers`. `GIT_LFS_SKIP_SMUDGE=1` добавлен — кэш клона 7.8 МБ вместо 1.7 ГБ.
Оговорка в README: `setup.py` использует `distutils`, удалённый в Python 3.12;
на нашем 3.11 работает, запасной путь описан.

**Решение 5.6.2 — VLM-бэкенд стал работоспособен.** `transformers` 4.46.3 → **4.49.0**
(проверено: `qwen2_5_vl` даёт 404 на v4.48.3 и 200 на v4.49.0), `tokenizers`
0.20.3 → 0.21.0, добавлены `bitsandbytes==0.45.5` и `accelerate==1.4.0`.
Ограничение `gigaam → torch<=2.5.1` не тронуто: `transformers` торч сверху
не пинит. Весь набор — 103 пакета вместе с git-пином — резолвится без конфликтов.
**Задача 4.8 перестала быть мёртвой опцией.**

**Решение 5.6.3 — `.dockerignore` положен как `docker/Dockerfile.dockerignore`.**
BuildKit читает `<Dockerfile>.dockerignore` раньше корневого, поэтому файл
остаётся в зоне владения агента. Проверено пробной сборкой: контекст
**279 МБ → 956 кБ**. Ограничение записано прямо в файле: legacy-билдер
(`DOCKER_BUILDKIT=0`) его игнорирует.

**Решение 5.6.4 — WARN → FAIL в сквозной проверке nvdec.** Это ровно та проверка,
ради которой существует `verify_env.sh` и формулировка задачи 1.1; предупреждение
вместо отказа обесценивало её.

**Тесты стали поведенческими.** Вместо проверки подстрок скрипт теперь реально
запускается с подменённым `PATH` и заглушками `nvidia-smi`/`ffmpeg`/`ldconfig`:
пустое окружение → код 1 и `[FAIL]`; `-hwaccels` без cuda → `[FAIL]`; энкод падает →
`[FAIL]` и отсутствие `[WARN]`. GPU для этого не нужна.

**Одна находка аудита отклонена, и агент прав.** Аудитор написал, что комментарий
приписывает `gigaam` пины onnx/onnxruntime, которых у него нет. Проверка по
`pypi.org/pypi/gigaam/0.1.0/json`: `requires_dist` содержит `onnx==1.17.0`
и `onnxruntime==1.17.3` без extra-маркера. Комментарий был верен; агент расширил
его до полного списка и указал источник.

### 5.7 Правки core — закрыты A1–A5 и ключевые P2, граф стадий заменил цепочку

**Граф стадий реализован.** `STAGES` = `audio → frames → slides → ocr → glossary →
vad → asr → punctuation → merge`, линейная цепочка заменена на `STAGE_DEPENDENCIES`,
где `audio` и `frames` — корневые (`parents=()`). `Cache.stage(..., parents=...)`
и `Cache.chain(..., dependencies=...)` с проверкой топологического порядка.
Решение 2.1.1 официально отменено (см. раздел 5.5).

Тонкость, которую агент заметил и правильно оставил как есть: смена `frames_fps`
всё равно инвалидирует `asr` — но не через кадры, а потому что ASR потребляет
глоссарий (`asr ← glossary ← ocr ← slides ← frames`). По D2 это верно.

**A1/A2 — валидация бэкенда шла по `available_backend_names()` вместо
`list_backend_names()`.** Одна подмена давала сразу два дефекта: опечатка в имени
бэкенда не отвергалась (пайплайн стартовал), а `--help` никогда не печатал имена
и врал «реестр ещё не доступен». Теперь печатает `hybrid, paddle, vlm` и
`gigaam-v2, whisper-large-v3`, а тест идёт по реальному `--help` через subprocess.

**A3 — код возврата.** Непустой список невыполненных стадий даёт **код 1**;
стадия без результатов предшественниц не запускается вслепую, а уходит в `missing`.

**A4 — `unload()` после каждой стадии-держателя модели.** Введён
`_STAGE_MODEL_HOLDERS` (ocr, asr, punctuation) вместо прежнего списка. Для `vad`
держателя нет намеренно: silero грузится внутри функции и отпускается по выходу.

**A5 — связка «поле конфига → стадия» закрыта таблицей на 32 параметра**
плюс тест, который не даёт добавить новое поле конфига, не прописав ему стадию.

**Решение 5.7.1 (главная сессия) — контракт-тест «YAML ↔ дефолты модулей стадий»
одобрен, несмотря на возражение исполнителя.** Агент отметил, что такой тест
свяжет его зону с именами полей чужих пакетов и будет краснеть при переименовании.
Именно это и нужно: категория уже дала три случая (`slide_ocr.backend` указывал
на несуществующий бэкенд, `hamming_threshold` 8 против подобранных 44, ещё шесть
значений выровнены руками), и все три раза расхождение было тихим — код работал,
значения были неверные. Красный тест при переименовании дешевле молчаливого
разъезда. Требование к падению: печатать конкретную пару «поле YAML ↔ поле модуля»
и оба значения.

**Решение 5.7.2 (принял агент) — `default=str` в сериализации ключа кэша убран,**
несериализуемый параметр теперь даёт `CacheError` с внятным текстом. Молчаливое
приведение к строке давало недетерминированный ключ для `set` и объектов —
6 разных ключей в 6 процессах, то есть кэш не переживал перезапуск.

**Решение 5.7.3 (принял агент) — в `stat`-идентичность входа добавлены
`ctime_ns` и `inode`.** Правка содержимого с восстановленным mtime больше
не даёт ложный хит.

**Отклонено агентом с обоснованием (согласен):** уборка `.tmp` — на корректность
не влияет (это признал сам аудит), а безопасная уборка требует политики возраста
и защиты от гонки с параллельным прогоном, который прямо сейчас туда пишет.

### 5.8 Аудит slide-extraction — 3×P0, 4×P1, 7×P2

Зона при этом зелёная (36 passed) — ни одна находка существующими тестами
не ловится.

**Порог подтверждён независимой методикой.** Аудитор проверял не phash, а
пиксельный MAE: внутри групп ≤5.0, на границах ≥6.9, распределения не пересекаются.
Все числа автора сошлись: область `Rect(56,56,1256,656)`, максимум дописывания 42,
минимум смены слайда 46, пороги 43…46 дают одни и те же 49 групп, 43 логических
слайда, те же 6 лишних делений (найдены независимо по монтажам), склеек нет.
Оговорка: 44 — середина зазора, а design D4/Risks требует смещения в сторону
дробления; вопрос передан агенту правок с требованием обосновать данными.

> **P0 №1 — кроп теряет рукописный ответ.** `min_brightness=150` привязан
> к светлому полю шаблона, поэтому область обрезается по белой карточке,
> а лектор дописывает ответ ЗА её пределами: t=1840 «Отв: −5; 0»,
> t=3613 «Ответ: 2», t=4677 «Ответ: 3, x₁=3, x₂=−4» — этих ответов в PNG нет.
>
> Это бьёт в основание D5. Рукописное сознательно не распознаётся, и единственный
> способ добраться до него — ссылка на PNG. Если PNG обрезан так, что ответа в нём
> нет, стратегия «честный пропуск вместо галлюцинации» не работает: содержание
> теряется молча, а транскрипт выглядит полным.

> **P0 №2 вместе с P1 №6 — длительность оказалась плохим признаком.**
> Интервалы слайдов поглощают отсутствие демонстрации (241 с у 24 слайдов),
> спековый «разрыв» не порождается никогда, а гистерезис `min_absence_s=20`
> съедает настоящее выключение: **2399–2412 (13 с) — это выключение по-настоящему**,
> так оно записано и в манифесте, и в разведке 2.7, и именно оно служит контрольной
> точкой задачи 3.2.
>
> То есть решение 5.2 («все артефакты ≤13 с, единственное выключение 60 с»)
> опиралось на неверную классификацию: 13-секундный интервал был отнесён
> к артефактам, хотя разведка называла его настоящим выключением. Запас порога
> оказался ×1.54, а не ×3.
>
> Агенту правок предложено искать признак по существу, а не по длительности:
> при переключении слайда область возвращается на том же месте, при настоящем
> выключении тайлы перестраиваются на весь кадр. Проверить на эталоне, а не принять
> на веру.

**P0 №3:** `extract_slides(config=…)` молча игнорирует `max_spread`,
`min_brightness`, `min_absence_s`, `hash_size` — те самые четыре параметра,
которые core поднял из констант в конфиг (решение 5.2.3). Мёртвые ручки:
`min_brightness=250` и `max_spread=0.0` не меняют результат.

**P1 №4:** тест «склеек нет» с порогом 80 при реальном максимуме 56–64 не может
упасть — аудитор подставил пороги 47/59/65, реально склеились 1–3 разных слайда,
тест остался зелёным.

**P1 №5:** нет ни диагностики при неуверенном детекте, ни ручного задания области.
Это дословное требование design → Risks. Сейчас на тёмных слайдах стадия молча
отдаёт `[]`.

### Решение 5.9 — второй обрыв по лимиту и порядок работы

Второй раз агенты легли на лимите сессии (сброс 12:10). Возобновлены через
`SendMessage`. Держу не более двух-трёх агентов одновременно: пропускная
способность всё равно упирается в лимит, а при обрыве теряется меньше работы.

### 5.10 core — подключены все девять стадий, контракт-тест дефолтов готов

Полный прогон: **417 passed, 3 skipped**.

**Решение 5.10.1 (принял агент) — приоритет точки входа пакета над адаптером.**
`_stage_callable` сначала ищет `run_<stage>_stage` / `run_stage` в самом пакете
и только потом берёт адаптер в `cli.py`. Как только пакет объявит свою точку
входа, она победит без правок в оркестраторе. Это снимает необходимость
переделывать пять пакетов ради соглашения, придуманного параллельно с ними
(открытый стык из решения 2.1), и оставляет путь к «правильному» состоянию
открытым.

**Решение 5.10.2 (принял агент) — несовпадение API стадии не роняет прогон
трейсбеком.** `TypeError`/`AttributeError`/`KeyError` из стадии превращаются
в сообщение «нужна точка входа `run_X_stage(ctx)` в самом пакете», стадия уходит
в `missing`, код 1. Введено специально потому, что `slide_extraction` правился
параллельно и его API мог поменяться под оркестратором.

**Сквозной прогон на 6-секундном клипе состоялся:** `audio → frames → slides`
отработали по-настоящему (5 слайдов), `ocr` остановился с внятным «бэкенд hybrid
недоступен: paddle/pix2tex не установлены», код 2. Повторный прогон дал
3 попадания в кэш и не декодировал заново. Это первое подтверждение, что
оркестрация и кэш работают на реальных данных, а не только в юнит-тестах.

**Контракт-тест дефолтов** параметризован по 24 полям и различает два вида
падения: расхождение значений (печатает обе стороны и подсказку) и переименование
поля (`AttributeError` с явным «переименован или удалён в пакете стадии»).
Отсутствие пакета даёт skip, чтобы не красить набор на чужой машине.

**Решение 5.10.3 (главная сессия) — `glossary_stopwords` получает поле
в `GlossaryConfig`, ключ из конфига не убираем.** design → Risks требует
возможности ручного вмешательства, когда автоматика ошибается; эвристика
отсечения оформления вероятностная, и на чужом шаблоне ей нужен аварийный выход.
Условие: стоп-лист остаётся пустым по умолчанию и никогда не заполняется под
конкретную платформу в коде — иначе вернётся ровно тот хардкод, который design
запрещает.

**Открытый пункт (не срочный):** PNG слайдов копируются в `<out-dir>/slides/`
адаптером в `cli.py`, чтобы ссылки в Markdown пережили снос кэша. Место спорное
(это скорее забота `write_transcript`), но поведение верное — оставляю как есть.

### 5.11 Стоп-лист глоссария подключён

`GlossaryConfig.stopwords` добавлен, сравнение по нормализованному ключу
(достаточно одной словоформы; фраза вырезает и себя целиком, и свои слова).
В docstring зафиксировано, что это ручной аварийный выход, а не замена эвристике,
и что в коде он никогда не заполняется под конкретную платформу.

Строку `stopwords=tuple(cfg.glossary_stopwords)` в `cli.py` дописал сам:
агент core в этот момент простаивал, правка в одну строку, гонять ради неё
отдельный заход дороже. Полный прогон после правки: **420 passed**.

### 5.12 Правки slide-extraction — закрыты все P0/P1 и пять P2

> ### ⚠ Решение 5.2 из раздела 5 УСТАРЕЛО, читать с учётом этой записи
>
> Там записано: `min_absence_s = 20 с`, «44 артефакта переключения ≤13 с,
> единственное настоящее выключение 60 с», порог hamming 44, 49 групп.
> Всё это пересмотрено ниже на основании проверки глазами.

**Кроп (P0 №1) — три потерянных ответа теперь в PNG.** Прямоугольник по маске
стал только ядром: `expand_to_static_border` расширяет его до тёмного промежутка
между плитками видеосетки. Порог темноты берётся из самого кадра, а не из цвета
шаблона — привязки к оформлению платформы нет. Область стала всей плиткой
демонстрации 16:9. Агент проверил глазами, все три ответа видны: t=1840
«x = ±4 Отв: −5; 0»; t=3613 «Ответ: 2 (не уд. ОДЗ)»; t=4677 «Ответ: 3 … x₁=3 x₂=−4».

Остаток, зафиксированный честно: на интервалах 5115–5392 срезаны левые ~56 px
рамки; рукописи там нет, причина не разбиралась.

> ### Переключение слайда против выключения демонстрации — моя гипотеза
> опровергнута данными
>
> Я предложил агенту признак «при переключении область возвращается на то же
> место, при выключении тайлы перестраиваются». Агент проверил на эталоне:
> **при каждом из 45 провалов тайлы перестраиваются на весь кадр** (глазами),
> а после контрольного 2399–2412 область возвращается на то же место с IoU 1.0 —
> ровно как после обычной смены слайда.
>
> Вывод: **на этой платформе это одно и то же событие.** Нет двух классов, которые
> надо различать, — есть 45 настоящих отсутствий демонстрации. Сверка с манифестом:
> все 33 его `no_demo_intervals` найдены; ещё 12 провалов значились у разведки только
> в `slide_switches_s`, и глазами в каждом одна видеосетка — это тоже настоящие
> отсутствия. То есть ручной подсчёт для задачи 3.3 уточнён с 33 до 45 проверкой
> каждого случая, а не подгонкой.
>
> Следствия: `min_absence_s` 20 → **2** (гасится только одиночный кадр, самый
> короткий настоящий провал 3 с); группа кадров режется на провале. Итог на
> эталоне: 90 интервалов раскладки, 45 без демонстрации, 89 переходов, t=2400 —
> отсутствие, ни один интервал манифеста не лежит внутри слайда.

**Порог hamming 44 → 43.** Расширенный кроп изменил расстояния: дописывание
маркером внутри непрерывного показа — до 30, слабейшая настоящая смена слайда — 56.
Середина зазора 30|56 — это 43; D4/Risks запрещает заходить выше середины,
в сторону склейки. Любой порог 31…46 даёт те же **53 группы**. Все 49 границ,
подтверждённых аудитом, сохранены — новых склеек нет; 4 новые границы — тот же
слайд с обеих сторон. **Логических слайдов по-прежнему 43**, остальные 10 групп —
лишние деления того же содержимого, что D4 допускает сознательно.

**Тест «склеек нет» (P1 №4) переведён на пиксельный MAE:** внутри групп максимум
3.96, на границах минимум 5.94, предел 5.0. Дополнительно ловит 30+ искусственных
склеек соседних групп — просто поднять порог на этой записи уже нельзя, потому что
провал демонстрации теперь режет группу при любом пороге.

**Диагностика и ручная область (P1 №5).** WARNING с числами (перцентили яркости
и разброса против порогов, заполнение, площадь и стороны региона) на каждое окно
без области и на пустой итог, с подсказкой задать область вручную. Параметр
`region: Rect` в `extract_slides` и `analyze_layout`. Поле конфига и флаг CLI
поручены core.

**Проверка «падал бы до правки»:** в scratchpad собрана копия пакета со старым
кодом и старым YAML — на ней 19 новых быстрых тестов и 8 эталонных падают.

> **Риск для задачи 7.5, записываю заранее.** 45 отсутствий демонстрации означают,
> что транскрипт будет резаться на 45 коротких секций «речь вне слайдов» по 3–13 с,
> плюс 10 лишних делений одного слайда. Для сборки это корректно (6.1 не теряет слов),
> но для чтения как исходника конспекта может оказаться шумно. Проверяется
> только на реальном транскрипте. Кандидат на правку формата: склеивать секцию
> вне слайдов короче порога с соседней, если слайд до и после — одно содержимое.

### 5.13 core — флаг `--slide-region`, дефолты синхронизированы

Полный прогон: **450 passed**.

`--slide-region X,Y,W,H` задаёт область демонстрации вручную (требование design →
Risks), значение `auto` сбрасывает её на автодетект. Мусор отвергается argparse
с кодом 2 и объяснением формата. Поле входит в ключ кэша стадии `slides`: смена
области пересчитывает ровно `slides` и его потомков, `audio`/`frames`/`vad` не
трогаются. Дефолты `min_absence_s` 2.0 и `hamming_threshold` 43 выровнены
с YAML — контракт-тест, одобренный в 5.7.1, поймал рассинхронизацию сразу,
то есть сработал ровно по назначению с первого же раза.

Проброс конфига в стадию проверен шпионами на внутренних функциях
`slide_extraction`: туда приходят именно значения из конфига. Это закрывает
мёртвые ручки из находки P0 №3 аудита slide-extraction со стороны оркестратора.

---

## 6. Итерация 2 аудита

Правило из запроса пользователя: аудит → правки → аудит, пока не останется
замечаний; после трёх итераций правится только критичное.

Итерация 2 сознательно уже первой: аудитор проверяет, что закрытые находки
**воспроизводимо** закрыты (исходный сбой повторяется на текущем коде — и не
проявляется), ищет регрессии, внесённые правками, и оценивает отклонённые P2
по фактам. Полного повторного прочёсывания нет — оно уже было и стоило двух
обрывов по лимиту.

### 6.1 Итерация 2 аудита transcript-assembly

Закрытие итерации 1: **6 из 7 P0/P1 подтверждённо закрыты** (исходный сбой
воспроизводился заново на текущем коде). Полнота 6.1 и детерминированность 6.6
без регрессий — около 200 тыс. случайных входов, два `PYTHONHASHSEED`.
Настоящие филлеры по-прежнему удаляются — консервативность не выродилась
в «не чистит ничего».

Не закрыто:

- P1-7 частично: тест клэмпа 6.2 ловит снятие правой границы, но не левой.

Новое:

- **P1 — слово нулевой длительности всегда уходит в «Речь вне слайдов».**
  Слайд выводится дважды, с повторной врезкой и картинкой, заголовки идут назад.
  Не регрессия правок — было изначально, итерация 1 пропустила. ASR такие слова
  пропускает с предупреждением, на реальной записи они возможны.
- **Две регрессии, внесённые нашими же правками чистки:** «Мы мы мы» → «Мы мы»
  (дедуп не доводится до конца) и потеря/повисание запятых при удалении повтора.
  Аудитор записал как P2; **поднимаю до обязательных к правке** — это порча текста,
  которую мы сами внесли, а не спорная эвристика.

**Решение 6.1.1 (главная сессия) — правило «однобуквенные токены вне дедупа»
отдано на усмотрение исполнителя.** Цена правила — перестали чиститься «в в»
и «я думаю я думаю». Предложено сужение: правило вводилось ради математических
обозначений, а это латинские буквы и цифры (`n`, `x`, `2`); русские однобуквенные
служебные слова формулами не являются. Но оставить как есть — тоже допустимо:
сохранённый лишний повтор безвреден, удалённое содержание — нет. Условие для
сужения: все математические контрпримеры итерации 1 обязаны остаться зелёными.

**Одно отклонение итерации 1 оспорено по фактам.** Агент отказался от
`Path.resolve()` в relpath, сославшись на детерминированность. Аудитор показал,
что `relpath` и так зависит от cwd, а `resolve()` эту зависимость как раз
убирает — обоснование было перевёрнуто. Серьёзность остаётся P2 (через CLI
сценарий недостижим); агенту поручено пересмотреть обоснование.

Агент правок итерации 2 запущен.

### Решение 6.2 — повторные аудиты на Sonnet, потолок одновременных агентов поднят

Повторные аудиты slide-ocr, media-ingest и core+docker запущены **на Sonnet**,
сразу все три, не дожидаясь освобождения мест.

Обоснование: итерация 2 — это в основном проверка воспроизведением по готовому
списку находок и поиск регрессий в известных местах, а не открытый поиск. Для
такой работы Sonnet достаточен, а расход квоты Opus заметно ниже — это ровно та
квота, что дважды обрывала работу. Итого одновременно 6 агентов, из них 3 на Opus
(аудиты speech-transcription и slide-extraction, правки transcript-assembly).

Для core+docker аудитор нацелен в первую очередь на граф стадий: **недостающая
зависимость в `STAGE_DEPENDENCIES` даёт ложный хит кэша со старыми данными**,
это худший из возможных дефектов слоя кэша — пайплайн молча отдаёт устаревший
результат. Лишняя зависимость — всего лишь лишний пересчёт.

> **Поправка к решению 6.2.** Запущены только **два** из трёх повторных аудитов
> (media-ingest и core+docker). Запуск аудита slide-ocr отклонён классификатором
> разрешений среды. Не повторяю его сразу тем же вызовом: отложен до освобождения
> мест, когда одновременно будет работать меньше агентов. Одновременно сейчас 5.

### 6.3 Итерация 2 аудита speech-transcription

Подтверждено воспроизведением: **10 из 11** закрытых находок. Нарезка кусков
≤19.98 с встык, абсолютные таймкоды, мутант `offset_s=0` роняет тесты, `()`
против `None` у VAD. **Пик памяти на 91-минутном WAV измерен независимо:
533–575 МБ** — заявленные 560 МБ подтверждены.

> ### ⚠ P0 — пунктуация не работала бы вообще, ни на одном куске
>
> `SbertPuncCaseRu._load` зовёт `SbertPuncCase.from_pretrained`, а у пакета
> с пинованного коммита такого метода нет: это `nn.Module` с `__init__()` без
> аргументов. Каждый кусок падал бы `AttributeError`, текст уходил бы без
> пунктуации — и из-за громкого отказа это хотя бы было бы видно, но только
> в одном из путей (см. ниже).
>
> Аудитор воспроизвёл на **настоящем коде пакета**, скачанном с HF, заменив
> torch/transformers заглушками. Итерация 1 этого не поймала, потому что фейковый
> пунктуатор в тестах был удобным и имел `from_pretrained`. Урок записываю
> явно: фейк должен повторять реальную сигнатуру зависимости, иначе тест
> проверяет наше представление о библиотеке, а не саму библиотеку.

> ### P1 — громкий отказ и очистка памяти не доходили до боевого пути
>
> Правки итерации 1 положили предупреждение об отказе пунктуации и очистку
> кэша WAV в `transcribe_speech`. Но CLI этот метод не зовёт — адаптеры в
> `cli.py` вызывают функции пакета напрямую. В боевом прогоне отказ оставался
> тихим, а 351 МБ сэмплов висели в памяти после ASR. Регрессия от кэша WAV.
>
> **Решение 6.3.1 (главная сессия).** Не править адаптеры в `cli.py`, а объявить
> точки входа `run_vad_stage` / `run_asr_stage` / `run_punctuation_stage`
> в самом пакете. Оркестратор специально сделан так, что точка входа пакета
> побеждает адаптер (решение 5.10.1) — это первый случай, когда этот механизм
> используется по назначению. Форма нагрузок берётся из текущих адаптеров,
> иначе сломается `merge`. Проверка — сквозь CLI, а не функцией пакета.

> ### P1 — GigaAM не может получить глоссарий, и это расхождение с design
>
> Design D2 и формулировка задачи 5.3 предполагают передачу глоссария в GigaAM
> как hotwords. **`gigaam 0.1.0` hotwords не принимает** — подтверждено двумя
> независимыми аудитами. `models`-тест 5.3 был бы на целевой машине всегда красным.
>
> **Решение 6.3.2 (главная сессия).** GigaAM честно объявляет
> `supports_glossary = False`; для него срабатывает деградация 5.5 (прогон без
> глоссария с предупреждением); тест 5.3 для GigaAM переписан на ожидание этой
> деградации. Для Whisper глоссарий через `initial_prompt` остаётся.
>
> **Сознательно НЕ сделано** — постобработка «подогнать написание распознанных
> слов под глоссарий» (нечёткое сопоставление с сохранением таймкодов). Это
> достигло бы цели D2 другим механизмом, но это новый механизм сверх спеки.
> **Выносится пользователю как предложение, см. итоговый раздел.**
>
> Следствие: задача 5.3 в формулировке «термины распознаны в написании глоссария»
> для бэкенда по умолчанию (GigaAM) **невыполнима на пинованной версии модели**.
> Это вопрос к design, а не к реализации.

Прочие новые находки — 6×P2: разрез не всегда попадает в паузу (14 из 201
на лекции), частичный отказ пунктуации не громкий (поднят до обязательного),
устаревший кэш WAV при том же размере и mtime, остаточная потеря знаков
при выравнивании.

### 6.4 Итерация 2 аудита slide-extraction

Закрыто подтверждённо: **11 из 14** находок итерации 1. Все три рукописных ответа
видны в PNG целиком; четыре поля конфига реально меняют результат; тест
«склеек нет» ловит все 48 склеек разного содержимого; PNG детерминированы до байта;
скорость не просела (разбор записи 16.8 → 15.9 с, стадия 41.7 → 44.8 с).

**Главные утверждения ревизии проверены независимо и подтверждены:**

- «Все 45 провалов настоящие» — аудитор посмотрел все 45, три самых коротких
  покадрово. В каждом одна видеосетка, ни одного тёмного слайда или артефакта сжатия.
- Порог 43 — 30 это максимум дописывания **по всем 5136 парам** соседних кадров,
  а не по выборке. 53 группы, 43 логических слайда, все 49 старых границ на месте.

> **Оговорка к порогу, важная для переносимости.** На эталонной записи **ни одна
> настоящая смена слайда порогом не решается** — все 44 режутся по провалу
> демонстрации, и «56» измерено через провал. Для платформы, где слайд меняется
> без провала демонстрации, порог 43 **не проверен**. Это ограничение данных,
> а не дефект; выносится в итоговый отчёт.

> ### P1 — кроп не пересчитывается при смене размера окна внутри интервала
>
> Одна причина, три разных последствия, все найдены глазами:
>
> - `slide_046`, `slide_048` — срезан печатный текст;
> - `slide_014`–`016` — срезана нижняя рамка, **там, где лектор писал ответы**:
>   ровно тот класс потери, который закрывала P0 №1 итерации 1;
> - `slide_019`–`021`, `024` — в кроп попала полоса ~90 px **с узнаваемыми
>   лицами студентов**.
>
> Утверждение агента правок «на 5115–5392 срезаны только левые ~56 px, рукописи
> нет» опровергнуто.
>
> Лица в PNG — это не только качество кропа. Proposal прямо называет «в кадре
> ФИО студентов» причиной держать обработку локальной. PNG ложатся в выходной
> каталог рядом с `transcript.md` и уйдут туда, куда уйдёт конспект.
> Захват тайлов участников в кроп слайда недопустим независимо от того,
> видны ли ФИО.

Также P1: битый кадр в середине записи всё ещё роняет стадию `ValueError` —
отсев итерации 1 сделан только для выборки статистик, а не для покадровой
проверки присутствия.

**Решение 6.4.1 (главная сессия) — P2 «один тёмный кадр режет слайд на два»
поднят до обязательного.** После снижения `min_absence_s` до 2 с это прямая
дорога к лишним секциям в транскрипте, которые и так стоят в риске для 7.5
(раздел 5.12).

Агент правок итерации 2 запущен с требованием осмотреть глазами все перечисленные
слайды после правки.

### 6.5 Правки transcript-assembly, итерация 2 — тестов 51 → 68, полный прогон 467 passed

**Слово нулевой длительности** теперь относится к слайду по точке `start_s`:
точка ровно на переключении уходит к следующему слайду, на конце последнего —
к нему. Дифференциальный фаззинг (40 тыс. входов): 0 дублей секций, 0 нарушений
порядка, 0 слов внутри слайда в «Речи вне слайдов».

**Регрессии чистки закрыты:** «Мы мы мы» → «Мы». Если пунктуация копий различается
(«Это, это важно.»), повтор теперь **остаётся**: любое удаление теряет либо
заглавную, либо запятую, а создавать новый `Word` запрещает решение 2.4.2
(таймкоды не должны проходить через чистку). Консервативный выбор, согласуется
с design → Risks.

**Решение 6.5.1 (принял агент) — сужение правила однобуквенных токенов.**
Из-под защиты сняты «в, и, с, к, у, я» — снова чистятся «в в этом случае»,
«я думаю я думаю». Но «а» и «о» оставлены под защитой: так же произносятся
точки A и O. Заглавная пара («точка В В») тоже защищена. В математические токены
добавлены названия букв («бэ», «вэ», «цэ»…), иначе «отрезок бэ бэ один» резался бы.
Все математические контрпримеры итерации 1 остались зелёными. Это содержательнее
моей подсказки «латиница против кириллицы» — агент учёл, как буквы звучат вслух.

**Отклонено с обоснованием (согласен):** расширение интервала секции до интервала
слов (заголовки соседних секций начали бы перекрываться); `section.paragraphs`
в рендере (контракт хранит только строки без времени, а `contracts.py` менять нельзя).

**Спор о `Path.resolve()` разрешён проверкой, а не аргументами.** Прежнее
обоснование агента было неверным — но и аудитор ошибся: `resolve()` зависимость
от cwd не убирает. Агент проверил из `/tmp` и `/usr` — оба способа дают одинаковые
пути. Относительный путь по определению отсчитывается от cwd; при смеси
абсолютного и относительного пути любой правильный ответ зависит от cwd.
Через CLI сценарий недостижим. Отклонено, теперь с верным обоснованием.

**Агент сам отозвал своё утверждение из итерации 1** «каждое правило только
сужает удаление» — аудитор показал регрессии, утверждение было неверным.

> **Решение 6.5.2 (главная сессия) — последний случай удаления содержания
> закрывается сейчас, а не откладывается.** Открытым оставался P2 №4:
> «как бы вы доказали» → «вы доказали» в тексте **без пунктуации** при паузе ≥0,3 с.
> Аудитор дал P2, но по сути это удаление содержания — ровно то, что design → Risks
> запрещает. Без пунктуации единственный сигнал «вставка» — пауза, а пауза сама
> по себе омонимию не разрешает. Поручено самое консервативное правило:
> без пунктуации вокруг омонимичный оборот не удаляется никогда.
> Однозначные филлеры это не затрагивает.

> **Дополнение к решению 6.2.** Повторный аудит slide-ocr запущен со второй
> попытки — переформулированным заданием, без переопределения модели. Итерация 2
> теперь идёт по всем шести зонам.

### 6.6 transcript-assembly — последний случай удаления содержания закрыт

Омонимичный оборот («как бы», «так сказать», «скажем так», «это самое») теперь
удаляется только при трёх условиях одновременно: не открывает предложение,
обособлен запятой или тире **с обеих сторон**, и с обеих сторон пауза ≥0,3 с.
Одна пауза вставкой больше не считается — в тексте без пунктуации такой оборот
не удаляется никогда. Однозначные филлеры и самоповторы правило не затрагивает
(«ээ ну вот смотрите мы мы рассмотрим э предел» → «смотрите мы рассмотрим предел»
и до, и после правки).

Два старых теста изменены сознательно: они закрепляли поведение, которое теперь
запрещено («как бы предел функции» без пунктуации ожидался удалённым).

Полный прогон: **500 passed, 0 failed**. По transcript-assembly известных открытых
замечаний не осталось; отклонённые P2 имеют обоснования, проверенные по фактам.
Запущена итерация 3 — только проверка, что закрытое закрыто и новых регрессий нет.

### 6.7 Итерация 2 аудита core + docker

**Все 8 P1 итерации 1 закрыты воспроизводимо** — каждая повторена мутацией
с побайтовым откатом или реальным прогоном CLI/Docker, ни одна не ожила.

**Граф стадий проверен и верен.** `STAGE_DEPENDENCIES` совпадает с D2 построчно;
недостающих зависимостей — то есть источников ложного хита кэша — **нет**.
Проверено чтением каждого адаптера и систематическим тестом по всем 32 полям.
Найдена одна лишняя зависимость: `asr` тянет `glossary` даже при
`use_glossary=False`. Это лишний пересчёт, а не ложный хит — **оставлено как
компромисс**: условная зависимость от значения конфига усложнила бы граф ради
редкого сценария.

**docker, новое:** VCS-пин пунктуатора и `GIT_LFS_SKIP_SMUDGE=1` стоят на верной
строке `RUN`; `.dockerignore` не исключает нужное сборке; подъём `transformers`
до 4.49.0 не ломает `pix2tex` и `gigaam` — подтверждено метаданными PyPI
и совместным резолвом.

> **P0 — попадание в кэш стадии `merge` не восстанавливает `transcript.md`
> на диске.** Удалить выходной файл, оставив `.cache`, перезапустить — код 0,
> файла нет. Тот же класс дефекта, что A3 итерации 1 (успешный код без результата),
> только проникший через кэш.

Также взяты в правку два P2: старые PNG слайдов не убираются из `<out-dir>/slides/`
при меньшем числе слайдов (после правок кропа число групп уже менялось 49 → 53,
мусорные картинки рядом с транскриптом запутают человека, пишущего конспект);
`--from-stage` вместе с `--no-cache` проваливается с обманчивым «нет в кэше».

### 6.8 Итерация 2 аудита media-ingest — зона закрыта

**Все 7 находок итерации 1 (1×P0 + 6×P1) подтверждённо закрыты воспроизведением:**
P0 — счётчиком процессов ffmpeg через CLI; смещение дорожки — на собственном
синтетическом файле аудитора; `%` в пути — на 7 вариантах спецификаторов;
мутации тестов точности меток и `resolve_hwaccel` падают; `.old`-мусора нет.

Регрессий от разделения `audio`/`frames` нет: независимость ячеек подтверждена
через CLI, `aresample` не сдвигает и не рвёт звук, `cwd=tmp_dir` не ломает
относительный путь ко входу. Отклонения P2 №10 и №11 признаны обоснованными
по фактам.

**В зоне media-ingest новых замечаний нет — цикл аудит → правки для неё завершён
на итерации 2.**

> **P1, найденный этим аудитом, но в зоне core:** аудио-режим (файл без видео)
> через CLI обрывает весь прогон на стадии `slides` с кодом 2. `frames` корректно
> отдаёт `None`, но адаптер `slides` бросает `PipelineError`, и `vad`/`asr`/`merge`
> не запускаются вообще. Спека media-ingest требует аудио-режим с результатом.
>
> Поручено core. Условие, добавленное главной сессией: в аудио-режиме OCR-бэкенд
> не должен даже проверяться на доступность — иначе файл без видео упадёт на машине
> без PaddleOCR, хотя OCR ему не нужен.

### 6.9 Итерация 3 аудита transcript-assembly — замечаний нет, зона закрыта

Всё закрытое подтверждено воспроизведением на текущем коде: слово нулевой
длительности не дублирует секцию; обе половины клэмпа 6.2, сломанные аудитором
на копии, роняют ровно свой тест; «Мы мы мы» → «Мы» без потери запятых;
«в в» и «я думаю я думаю» чистятся, математические обозначения сохраняются;
«как бы» без пунктуации остаётся при любых паузах.

Регрессий нет: полнота 6.1 держится на 60 тыс. фаззинг-входов (включая нулевые
длительности и слова на границах), детерминированность — идентичный sha256
на 15 тыс. вызовов при трёх `PYTHONHASHSEED`. Новые обоснования отклонённых
P2 №8, №11, №16 проверены по коду и признаны фактически верными.

**Цикл аудит → правки для transcript-assembly завершён на итерации 3.**

Статус задач 6.2 и 6.4 при этом **не меняется** — они остаются неотмеченными.
Код готов и проверен, но формулировки требуют проверки на эталонной записи
(«на моменте переключения слайда эталонной записи», «на десятиминутном монологе
эталонной записи»), а для этого нужны настоящие слова из ASR, то есть веса моделей.

### 6.10 Правки speech-transcription, итерация 2 — тестов 53 → 72

**P0 закрыт на реальном API пакета.** Агент скачал код `sbert_punc_case_ru` с коммита
`f778dc6c` (без весов) и прочитал API: `class SbertPuncCase(nn.Module)`, `__init__()`
без аргументов, веса грузятся внутри конструктора, дальше `.to(device)`
и `.punctuate(text) -> str`. Метода `from_pretrained` нет. Загрузчик переписан;
**фейк в тестах теперь повторяет реальную сигнатуру и сам проверяет, что
`from_pretrained` у него нет** — урок из 6.3 превращён в проверку. Дополнительно
адаптер прогнан на настоящем коде пакета с заглушками torch/transformers:
пунктуация применена, предупреждений 0.

**Боевой путь через CLI.** В пакете объявлены `run_vad_stage`, `run_asr_stage`,
`run_punctuation_stage`; оркестратор берёт их вместо адаптеров (решение 6.3.1
сработало). Громкий отказ перенесён в сам `restore_punctuation`, чтобы его нельзя
было обойти ни одним путём. Проверено реальным `cli.main` на `clip_speech.mp4`
с адаптерами `cli.py`, подменёнными на `AssertionError`: вызваны именно точки
входа пакета, кэш WAV пуст после ASR, маркер отказа есть и в логе (ERROR),
и в результате стадии. Отдельным процессом настоящий бинарник тоже взял точку
входа пакета.

**GigaAM и глоссарий — по решению 6.3.2:** `supports_glossary = False`, срабатывает
деградация 5.5, `models`-тест 5.3 переписан на ожидание деградации.

**Частичный отказ пунктуации теперь громкий:** «ПУНКТУАЦИЯ ВОССТАНОВЛЕНА
ЧАСТИЧНО: N из M кусков» и ERROR. Ответ модели «текст без изменений» тоже
считается отказом.

Прочие P2 закрыты: в ключ кэша WAV добавлены inode и ctime; разрез длинного
интервала ищет паузу во всей второй половине куска, а не в последних 0,75 с;
отдельно стоящие закрывающие знаки больше не теряются (2000 сгенерированных входов).

**Причины по P2, которые аудитор просил записать:**

- №10 **отклонена**: `normalize_core` и `_normalized_chars` одинаково отбрасывают `İ`,
  расхождения в коде нет; замену `i` на `İ` пинованная модель выдать не может.
- №11 **отложена**: переход с глобального пика на скользящий перцентиль в
  энергетическом VAD меняет алгоритм и требует подбора на эталоне. В образе стоит
  silero, энергетический VAD — только запасной путь.
- №14 **отложена**: любая минимальная правка переносит дефис на правое слово
  («-русски»). Пинованная модель слова не склеивает; правильная склейка — задача
  transcript-assembly.

Общий прогон: 533 passed, 1 failed — `test_models_are_unloaded_after_every_model_stage`
в зоне core. Агент доказал непричастность: с убранными его точками входа тест
падает так же. Причина — новая проверка `transcript.md`, которую core добавляет
прямо сейчас (правка P0 из 6.7).

Запущена итерация 3 аудита speech-transcription.

### Решение 6.11 — третий обрыв по лимиту

Легли все четыре работавших агента: правки core (в самом начале захода),
правки slide-extraction (на ожидании финального эталонного прогона), второй аудит
slide-ocr и третий аудит speech-transcription. Все четыре возобновлены через
`SendMessage` с напоминанием, на чём остановились. Агенту slide-extraction
отдельно поручено проверить консистентность зоны: обрыв мог прийтись на запись файла.


## 7. Смена режима: MVP важнее полировки

**Решение пользователя (единственное за всю сессию, остальное — автономно):**
«давай менее строгие аудиты — не критичное и сложное записывай в тех долг,
особенно когда много итераций, а то все токены потратим, а MVP не сделаем».

Что изменилось:

- Чинится только то, что ломает пайплайн на реальных данных или теряет/искажает
  содержание (P0/P1). P2, тестовая гигиена, отложенные улучшения — в
  **`TECH_DEBT.md`** (создан, туда перенесены все ранее отложенные и отклонённые
  пункты из аудитов, плюс то, что можно проверить только на целевой машине).
- **Третьи итерации аудита core и slide-extraction отменены.** Их правки
  итерации 2 закрывают только P0/P1 и завершаются тестами.
- Работающие аудиты (slide-ocr итерация 2, speech-transcription итерация 3)
  сужены до ядра: закрытие P0 и главного P1, новые находки — только P0/P1,
  остальное одной строкой «в тех долг».
- У core из пяти пунктов оставлены три: P0 (кэш `merge` не восстанавливает
  `transcript.md`), P1 (аудио-режим через CLI) и упавший тест. Уборка старых PNG,
  сообщение для `--from-stage --no-cache`, удаление мёртвых адаптеров — в долг.
- **Интеграционной проверкой вместо следующих итераций аудита становится
  сквозной прогон раздела 7** на эталонной записи.

Оглядываясь: строгость стоила трёх обрывов по лимиту сессии. Она нашла настоящие
дефекты — неработающий пунктуатор, удаление содержания из математической речи,
лица студентов в PNG, потерю рукописных ответов, — так что первые две итерации
окупились. Третья по уже стабилизированным зонам давала в основном P2.

### 7.1 core — итерация 2 закрыта, полный прогон 535 passed

- **P0 кэш `merge`:** стадия собирает `transcript.md` и PNG в своей ячейке кэша,
  а в `<out-dir>` их выкладывает общая функция публикации — одинаково для
  посчитанной стадии и для попадания в кэш. Страховка: `merge` выполнена,
  а файла на диске нет → код 1.
- **P1 аудио-режим:** файл без видео доходит до `transcript.md` с кодом 0, в нём
  только «Речь вне слайдов». OCR и глоссарий не вызываются вовсе — PaddleOCR
  такому файлу не нужен. Предупреждение о неполноте одно, в конце прогона.
  Проверено через CLI: m4a из lavfi, любое обращение к OCR роняет тест.
- **Тест выгрузки моделей** починен и ужесточён: теперь проверяет, что модель
  выгружается **до старта следующей стадии**, а не только к концу прогона —
  это и есть буквальное требование D7.

Три пункта, которые я при смене режима отправил в долг (уборка старых PNG,
отказ `--no-cache` с понятной причиной, удаление мёртвых адаптеров speech),
к приходу сообщения **уже были полностью готовы и зелёные** — агент их оставил.
Это правильно: выбрасывать готовый протестированный код ради режима экономии
бессмысленно. Адаптеры `slides`/`ocr`/`glossary`/`merge` остаются в `cli.py`
до появления точек входа в пакетах — это в `TECH_DEBT.md`.

### 7.2 Второй аудит slide-ocr (сокращённый) — два блокера для целевой машины

P0 №1–4 итерации 1 подтверждены закрытыми (на текущем коде и мутациями на копиях).

> ### Два P0, которые не дали бы OCR запуститься на целевой машине вообще
>
> 1. **Проверка весов ищет их в `~/.paddlex`, а образ кладёт в
>    `PADDLE_PDX_CACHE_HOME=/cache/paddlex`.** `hybrid` объявлялся бы недоступным
>    даже со скачанными весами — прогон не стартует.
> 2. **`paddleocr==3.2.0` из пина образа падает в конструкторе:
>    `ValueError: Unknown argument: show_log`.** Аудитор проверил на коде колеса.
>    Это отложенный в 5.1 пункт P2 №21 («аргументы конструктора PaddleOCR 3.x не
>    проверены без установленной библиотеки») — он оказался блокером.
>
> Второй раз за сессию один и тот же класс дефекта: **код написан против
> представления о сторонней библиотеке, а тест — против удобного фейка**
> (первый был пунктуатор, 6.3). Оба раза находку дало скачивание настоящего кода
> пакета. Агенту правок поставлено то же требование: прочитать реальную сигнатуру
> по колесу, фейк в тесте повторяет её.

**P1 взят в правку:** полностью неуверенный слайд выходит пустым и не помечается
ненадёжным — печатный текст с уверенностью <0.35 выбрасывается как рукописный.
Молчаливая потеря содержания: человек не узнаёт, что надо смотреть в PNG.

**P1 отправлен в долг с условием:** рукописное решение, где OCR увидел `=` или `+`,
по-прежнему уходит в pix2tex. Улучшение эвристики — в долг, но агент проверяет,
что такой фрагмент помечен `low_confidence` с видимой пометкой «сверьтесь
с изображением». Если помечен — это приемлемо для MVP: ссылка на PNG рядом,
пометка видна.

### 7.3 slide-extraction — итерация 2 закрыта, открытых пунктов нет

**Кроп теперь ищется заново по последним 5 кадрам каждой группы** — то есть по окну
демонстрации на момент репрезентативного кадра, а не на начало интервала.
Не удалось на 2 группах из 53 — там остаётся область интервала с WARNING
(на этих двух она верна).

**Осмотр глазами всех проблемных слайдов:** печатный текст целиком везде
(«Дробно-рациональное уравнение» на 046 и 048 полностью); ответы целиком
(на 021 «Ответ: −0,2», на 014 «0,13» внизу); рамка шаблона видна со всех четырёх
сторон; **лиц студентов и полос соседних плиток нет.**

Закрыто также: битый или отсутствующий кадр в середине записи считается кадром
без демонстрации с WARNING, а не роняет стадию; одиночный тёмный кадр больше
не режет слайд; кадр другого размера первым в выборке не портит область; ручная
область из конфига читается.

**Числа не изменились:** 53 группы, 43 логических слайда, границы групп те же.
Стадия на полной записи — 42 с. Полный прогон проекта — **535 passed**.

> **Находка из аудита slide-ocr, которая блокирует целевую машину, но лежит вне
> всех зон:** `docker/compose.yaml` ссылается на `python -m lecture_transcript.warmup`,
> а такого модуля нет. При этом образ по умолчанию работает с `HF_HUB_OFFLINE=1` —
> то есть без прогрева **веса моделей на целевой машине не появятся вообще**,
> и все бэкенды будут честно недоступны.
>
> **Решение 7.3.1 (главная сессия).** Это не долг, а блокер MVP. Нужен модуль
> `warmup`: с отключённым офлайн-режимом по очереди загрузить модели всех
> зарегистрированных бэкендов (OCR, ASR, VAD, пунктуатор) их же собственными
> загрузчиками в `/cache`, выгрузить. Поручается агенту core — **после** того, как
> slide-ocr закончит переписывать загрузчик PaddleOCR под реальный API 3.2.0,
> иначе прогрев будет вызывать загрузчик, который прямо сейчас меняется.

Сквозной прогон раздела 7 ждёт того же: правки slide-ocr меняют код стадий
`ocr` и `glossary`, которые прогон исполняет.

### 7.4 speech-transcription — итерация 3 (сокращённая), замечаний нет, зона закрыта

Проверено воспроизведением:

- **Пунктуатор работает на настоящем коде пакета** с коммита `f778dc6c`
  (заглушки только для torch/transformers): вход «привет мир тест» дал
  `'Привет мир, ТЕСТ.'`, `AttributeError` нет.
- **Боевой путь через CLI:** `cli._stage_callable` берёт `run_vad_stage` /
  `run_asr_stage` / `run_punctuation_stage` из пакета; маркер отказа уровня ERROR
  есть в реальном `cli.main` и при полном, и при частичном (1 из 3 кусков) отказе
  пунктуации.

По режиму MVP не перепроверялись (проверены тестами агента правок, но не
независимо): число разрезов не в паузу на полной лекции, ключ кэша WAV
по inode/ctime, регрессии поиска паузы. **Цикл аудит → правки для
speech-transcription завершён.**

### 7.5 slide-ocr — итерация 2 закрыта, блокеры целевой машины устранены

- **Кэш PaddleX:** веса ищутся там же, где их ищет сам PaddleX —
  `$PADDLE_PDX_CACHE_HOME/official_models`, без переменной `~/.paddlex/official_models`.
  Сверено по колесу paddlex 3.2.0 с точными строками. Служебные `locks/` и `func_ret/`
  за веса больше не считаются (заодно закрыт пункт долга).
- **API PaddleOCR 3.2.0:** сигнатура взята из колеса. Конструктор 3.x —
  `PaddleOCR(lang=…, use_textline_orientation=True)`, распознавание — `predict()`.
  Версия определяется по наличию `predict` у класса, ветка 2.x сохранена.
  **Фейк в тесте повторяет сигнатуру колеса**, включая `ValueError: Unknown argument`
  на неизвестном аргументе — урок из пунктуатора применён без напоминания.
- **Молчаливая потеря:** если гибрид отбросил хоть одну строку как рукописную,
  слайд помечается `unreliable` с видимой пометкой «Распознано неуверенно.
  Сверьтесь с изображением слайда.» Порог 0.35 не менялся.
- **Рукописное с `=`/`+`:** проверено, что фрагмент получает `low_confidence`
  и пометку `_(?)_` рядом с формулой. Для MVP приемлемо. Оговорка агента: фраза
  «сверьтесь с изображением» выводится только на весь слайд при ≥50% неуверенных;
  у одиночного фрагмента — только `_(?)_`.

Полный прогон: **539 passed**. **Цикл аудит → правки для slide-ocr завершён.**

---

## 8. Интеграция

Все шесть зон закрыты. Запущены параллельно две задачи, разблокированные
окончанием правок slide-ocr:

1. **Модуль `warmup`** (агент core) — прогрев весов всех выбранных бэкендов их же
   загрузчиками, с финальной проверкой доступности уже в офлайн-режиме.
2. **Сухой сквозной прогон на всей эталонной записи** (новый агент). Настоящий
   пайплайн — извлечение, слайды, VAD, кэш, граф, выгрузка, сборка — где заменены
   только OCR/ASR/пунктуация детерминированными фейками. Проверяет всё, кроме
   качества распознавания: разрешимость ссылок на PNG, полноту слов, порядок
   секций, число и длительности секций вне слайдов (риск 5.12), детерминированность,
   тайминги стадий на CPU, попадание в кэш при повторе, формат для задачи 7.5.

**Решение 8.1 (главная сессия) — скрипт сухого прогона кладётся в репозиторий
(`tools/dry_run.py`), а не только в scratchpad.** Это единственный способ проверить
интеграцию без GPU и весов; он пригодится после любой правки стадий, пока целевая
машина недоступна.

**Задачи 7.x сухой прогон НЕ закрывает** — их формулировки требуют настоящего
распознавания, VRAM и времени на GPU. Он закрывает риск, что на целевой машине
пайплайн упадёт на стыке стадий, а не на моделях.

## 9. Статус задач (для сверки с `tasks.md`)

Отмечено выполненными **28 из 45**. Каждая неотмеченная задача имеет готовый код
и тесты там, где их можно написать без весов; не выполнена именно та проверка,
которая записана в её формулировке.

| Задача | Почему не отмечена | Что нужно |
|---|---|---|
| 1.1 | `nvidia-smi` и nvdec в контейнере — нужна RTX 4060 | `docker/verify_env.sh` на целевой машине |
| 1.2 | `torch.cuda.is_available()` в чистом образе | то же |
| 4.2 | распознавание слайда t≈425 — нужны веса PaddleOCR | `pytest -m models` |
| 4.4 | формула «Пример 2» t≈4210 — нужны веса pix2tex | `pytest -m models` |
| 4.7 | глоссарий на эталонной записи — нужен настоящий OCR | прогон с весами |
| 4.9 | прогон в контейнере без сети (здесь эмулирован патчем сокетов) | контейнер без сети |
| 5.3 | нужны веса GigaAM; **плюс расхождение с design: `gigaam 0.1.0` не принимает hotwords** (6.3.2) | решение по глоссарию для GigaAM, см. `TECH_DEBT.md` |
| 5.4 | нужны веса Whisper large-v3 | `pytest -m models` |
| 5.8 | прогон в контейнере без сети | контейнер без сети |
| 6.2 | граница секций на эталонной записи — нужны настоящие слова ASR | прогон с весами |
| 6.4 | абзацы на 10-минутном монологе эталона — нужны настоящие слова ASR | прогон с весами |
| 7.1–7.6 | сквозной прогон с моделями, VRAM, время на GPU, чтение реального транскрипта, подбор порогов | целевая машина |

Задачи, закрытые с оговоркой, которую надо знать:

- **2.3** — извлечение кадров через nvdec: код поддерживает `hwaccel=cuda`,
  проверка из формулировки (число кадров и монотонность меток) выполнена на CPU.
- **3.3** — «число переходов совпадает с ручным подсчётом»: ручной подсчёт
  уточнён с 33 до 45 отсутствий демонстрации проверкой каждого случая глазами
  (5.12, подтверждено независимым аудитом 6.4).
- **3.7** — порог 43 подобран и подтверждён на эталоне, но на этой записи все
  смены слайда режутся провалом демонстрации; для платформ без провала порог
  не проверен (`TECH_DEBT.md`).

### 8.2 Модуль `warmup` готов — полный прогон 552 passed

`python -m lecture_transcript.warmup` снимает офлайн-режим только на время прогрева
(запреты телеметрии остаются), загружает модели выбранных бэкендов по одной
с `unload()` после каждой, печатает сводку OK/FAIL с причиной, код 1 при любом
отказе. `compose.yaml` вызывает модуль без аргументов — совпадает с реализацией,
закреплено тестом. Режимы: по умолчанию бэкенды из конфига, `--all`, `--with-vlm`.

**Решение 8.2.1 (принял агент, тонкое и верное) — список офлайн-переменных
читается разбором исходников, без импорта модулей.** `huggingface_hub` запоминает
`HF_HUB_OFFLINE` в момент собственного импорта; если импортировать модуль стадии
раньше, чем снят офлайн, снимать его потом бесполезно — прогрев бы молча ничего
не скачал.

**Решение 8.2.2 (принял агент) — загрузка через минимальные фиктивные вызовы.**
Пакеты не дают способа «загрузить модель без распознавания», а лезть в их зоны
агент не стал: OCR — `recognize()` пустого PNG 64×32 (у `hybrid` — обе части
отдельно, иначе на пустой картинке pix2tex не загрузится), ASR — секунда тишины,
silero — `detect_speech()` на тишине, пунктуатор — две слова.

**Оговорка, записанная в долг, а не в правку.** Финальная проверка «после прогрева
бэкенд доступен в офлайне» слабее, чем задумано: у pix2tex, пунктуатора, silero
и gigaam `check_availability()` смотрит только на установленные пакеты. Для MVP
это не блокер — успешная фиктивная загрузка при прогреве сама скачивает веса,
и сводка честно покажет FAIL, если загрузка не прошла.

### 8.3 Сухой сквозной прогон на всей эталонной записи — пайплайн работает

Настоящий пайплайн, где заменены только OCR/ASR/пунктуация детерминированными
фейками (`tools/dry_run.py`). **Дошёл до `transcript.md` на всей 91-минутной записи,
все 9 стадий, код 0, обходов в скрипте не понадобилось.**

| Что | Результат |
|---|---|
| Время первого прогона (CPU, macOS) | 355 с: frames 5 мин 16 с (videotoolbox), slides 36.7 с, audio 2.0 с, vad 0.2 с, merge 0.1 с |
| Пик RAM | 1.09 ГБ |
| Прогон из кэша | 0.17 с, попадание по всем 9 стадиям, `transcript.md` байт в байт |
| Детерминированность | два прогона с чистым кэшем на всей записи — одинаковые `transcript.md` и PNG |
| Полнота слов | 12 488 слов ASR = 11 377 в транскрипте + 1 111 удалённых филлеров и повторов |
| Ссылки на PNG | все 53 разрешаются, лишних PNG нет |
| Секции | 53 слайда, 45 «Речь вне слайдов» (44 короче 15 с, медиана 5 с) |

**P0 нет.** Два P1 — оба по удобству транскрипта, то есть по сути MVP.

> **P1 — двойная пометка ненадёжности** на 18 из 53 слайдов, одна вложенной
> цитатой. Одну кладёт slide-ocr в `SlideOcr.markdown`, вторую —
> рендер. Тесты каждой стороны подавали данные руками — связка не проверялась.
>
> **Решение 8.3.1 (главная сессия):** пометка — это представление, её владелец —
> рендер. slide-ocr убирает пометку из `markdown`, оставляя флаг `unreliable`.
> Добавляется интеграционный тест «реальная сборка `SlideOcr` + реальный рендер» —
> стадии друг друга не импортируют, но тестам это разрешено.

> **P1 — риск 5.12 подтвердился.** Один слайд «Линейное уравнение» разбит на
> 4 секции подряд (`slide_020.png` побайтово равен `slide_019.png`, секция длится
> 1 с). С настоящим OCR врезка повторилась бы 4 раза. Между слайдами — 44 вставки
> «Речь вне слайдов» по несколько секунд.
>
> **Решение 8.3.2 (главная сессия) — осознанное отклонение от буквы спеки.**
> Спека transcript-assembly требует «отдельные секции для речи вне слайдов».
> На эталонной платформе каждая смена слайда даёт провал демонстрации на 3–13 с
> (5.12) — это артефакт платформы, а не речь вне слайдов по смыслу. Правило:
> секция вне слайдов **короче 15 с** присоединяется к предыдущему слайду;
> длинные (первая минута записи, 60 с) остаются отдельными — для них требование
> спеки сохранено.
>
> Соседние секции слайдов **с одинаковым содержимым OCR** сливаются в одну;
> PNG, врезка и номер — от последнего слайда группы (D4). Пустой OCR у обоих
> одинаковым содержимым не считается. Полнота 6.1 обязана сохраниться.
>
> Почему в сборке, а не в slide-extraction: у сборки есть содержимое OCR —
> сигнал «одно и то же на слайде» по смыслу, а не по пикселям; slide-extraction
> остаётся верна D4 («лишний слайд заметен, потерянное решение — нет»).
> **Требует внимания пользователя** — это меняет формат транскрипта относительно
> спеки.

Попутно в ту же правку (дёшево и сильно влияет на чтение): первая строка OCR
в заголовке секции слайда — сейчас 98 заголовков без смысла; единый формат времени
`H:MM:SS` вместо смеси; заглавная буква в начале предложения **на этапе рендера**
(621 место после удаления «Ну вот»/«Ээ») — `Word` при этом не пересоздаётся,
решение 2.4.2 не нарушается. Длительность в шапке — правка в `cli.py`.

**Решение 8.3.3 — фейковый OCR в сухом прогоне** выдаёт текст по индексу слайда,
поэтому слияние одинаковых слайдов на нём не сработает. После правок прогон
повторяется с фейком, производящим текст из хэша содержимого PNG — побайтово
одинаковые кропы получат одинаковый текст, как это было бы с настоящим OCR.

### 8.4 Двойная пометка ненадёжности устранена

Пометка уровня слайда убрана из `SlideOcr.markdown`; её рисует только рендер
транскрипта по флагу `unreliable`. Флаг, пометки фрагментов `_(?)_` и ссылка
на PNG остались. Тест на размытый кроп теперь проверяет флаг, а в `markdown` —
только отсутствие пометки, чтобы дубль не вернулся. Полный прогон: **554 passed**.

### 8.5 Фейковый OCR сухого прогона выводится из содержимого PNG

Текст фрагментов теперь зависит от кропа, а не от номера слайда: светлая карточка
слайда уменьшается до 32×18 в оттенках серого, яркость квантуется до 6 уровней,
от этого берётся хэш. `slide_019` и `slide_020` получают одинаковый текст,
соседние разные слайды — разный; при сдвиге окна на 2 px хэш сохраняется
в 208 из 212 вариантов. На всей записи ожидается 2 слитые группы (53 → 51 секция).

Где сухой прогон слияние **не проверит** (фейк чувствительнее настоящего OCR):
слайд с дописанным рукописным ответом (20/21), дорисованный эскиз (27/28),
«Итоги лекции» 46–49 со сменой масштаба окна.

> **Поправка к решению 8.3.2, найденная благодаря этому отчёту.** Я задал правило
> «при слиянии PNG и врезка — от последнего слайда группы». Но настоящий гибридный
> OCR выбрасывает рукописное (D5), поэтому два соседних состояния слайда с одной
> печатной заготовкой и **разным рукописным решением** дадут одинаковый текст
> и сольются — а ранний PNG с первым решением исчезнет из транскрипта. Это ровно
> та потеря решения, от которой защищают D4 и D5. На эталоне реальный случай —
> 20/21: на 21 дописан «Ответ: −0,2».
>
> Исправленное правило: слитая секция — один заголовок, одна врезка OCR, но
> **ссылки на все различающиеся PNG группы**; побайтово одинаковые картинки
> показываются один раз. Лишние заголовки и повторы врезок уходят, содержание
> не теряется. Поправка отправлена агенту transcript-assembly во время его работы.

### 8.6 transcript-assembly — формат транскрипта под конспект

Полный прогон: **575 passed**. Мутационная проверка слияния и присоединения —
11 мутаций, все убиты.

**На эталонной записи** (кэш стадий до `merge`): секций **98 → 54**,
«Речь вне слайдов» **45 → 1** (осталось вступление 60 с), тема в заголовке у 44
слайдов (у 9 OCR пуст), ссылок на PNG по-прежнему 53, времени в формате `MM:SS`
128 → 0, строчных начал предложений 560 → 0.

Пример заголовка: `## Слайд 1 — Пример 1. Решите уравнение — 0:01:00–0:04:08`.

**Решение 8.6.1 (принял агент) — группировка одинаковых слайдов на уровне рендера,
а не структуры.** `TranscriptSection` вмещает один `slide`, контракт менять нельзя,
поэтому в `Transcript.sections` каждый слайд остаётся со своим PNG и интервалом,
а рендер выводит группу одним заголовком `## Слайды 19–20 — …`, одной врезкой
и ссылками на все различающиеся по содержимому PNG. Поправка из 8.5 соблюдена.
Минус: программный потребитель `Transcript.sections` группировку не видит —
для MVP это неважно, потребитель один, и это `transcript.md`.

**Решение 8.6.2 (принял агент) — порог присоединения 15 с, строгое «меньше».**
Провалы демонстрации длятся 3–13 с, самая короткая настоящая речь без слайда —
вступление 26 с. 0 выключает правило.

**Рендер снимает лишнюю цитату-пометку slide-ocr** в начале `markdown` —
пометка ненадёжности выходит ровно одна независимо от версии slide-ocr.
Параллельно slide-ocr уже убрал её у себя (8.4); защита с двух сторон безвредна.

> **Найдено попутно — P1 против D8.** Кэш стадии не учитывает версию кода.
> Агент поменял шаблон, прогнал на старом кэше и получил прежний `transcript.md`
> без правок, пока вручную не удалил `cache/merge`. D8 прямо обещает «смена
> шаблона вывода — инвалидирует только сборку», и кэш существует ради быстрых
> итераций по формату. Молча отдать устаревший результат — худший вид дефекта
> кэша. Поручено core: отпечаток исходников пакета стадии в ключе, инвалидация
> стадии и потомков, но не предков.

Остаток сухого прогона: слияние одинаковых соседних слайдов на эталоне проверено
только тестами — прошлый фейк OCR давал слайдам разный текст. Повторный сухой
прогон с фейком по содержимому PNG — после правки кэша, иначе он сам наткнётся
на ту же проблему.

## 10. Что требует решения пользователя

Всё остальное в этом файле решено автономно и может остаться как есть. Эти пункты
меняют поведение относительно спеки или design — их стоит прочитать.

1. **Короткая «Речь вне слайдов» присоединяется к соседнему слайду (8.3.2, 8.6.2).**
   Спека transcript-assembly требует отдельных секций для речи вне слайдов.
   На эталонной платформе каждая смена слайда даёт провал демонстрации 3–13 с,
   и без присоединения транскрипт распадается на 45 обрывков по несколько секунд.
   Секции короче 15 с присоединяются, длинные остаются. Выключается
   `transcript_assembly.short_offslide_section_s: 0`. Если согласны — стоит внести
   в спеку через `/opsx:update`.

2. **GigaAM не получает глоссарий (6.3.2).** Design D2 строится на подаче глоссария
   слайдов в ASR как hotwords, а `gigaam 0.1.0` hotwords не принимает. Сейчас
   GigaAM работает без глоссария с предупреждением; Whisper получает его через
   `initial_prompt`. Задача 5.3 для бэкенда по умолчанию на этой версии модели
   невыполнима. Варианты: (а) принять и поправить D2/5.3; (б) постобработка —
   подгонять написание распознанных слов под глоссарий нечётким сопоставлением
   с сохранением таймкодов; это новый механизм, не делал без апрува;
   (в) Whisper по умолчанию для лекций с плотной терминологией.

3. **Порог hamming 43 проверен только на платформе, где смена слайда идёт
   с провалом демонстрации (6.4).** На эталоне ни одна смена слайда порогом
   не решается — все режутся по провалу. На другой платформе вебинаров порог
   придётся перепроверить.

4. **Соседние одинаковые слайды группируются рендером, а не в структуре данных
   (8.6.1).** Контракт `TranscriptSection` вмещает один слайд; менять `contracts.py`
   ради этого не стал. Для `transcript.md` разницы нет.

### 8.7 Отпечаток кода стадии в ключе кэша — полный прогон 587 passed

Ключ стадии теперь включает хэш исходников её пакета (пути и содержимое `.py`,
файлы отсортированы, `__pycache__` пропускается); каталог хэшируется один раз
за прогон. Правка кода инвалидирует стадию и её потомков по графу, предков нет.
Сценарий «правка `transcript_assembly` → пересчитан только `merge`» закреплён тестом.

**Решение 8.7.1 (принял агент) — для стадий, идущих через адаптер в `cli.py`,
в ключ входит весь `cli.py`, а не отдельные функции.** Перечисление функций
поштучно рано или поздно забудет одну — и кэш снова молча отдаст старый результат.
Цена: любая правка `cli.py` пересчитывает `slides`, `ocr`, `glossary`, `merge`.
Уйдёт, когда эти пакеты объявят свои точки входа (пункт в `TECH_DEBT.md`).

Поле `transcript_assembly.short_offslide_section_s: 15.0` в конфиге, в таблице
«поле → стадия» и в контракт-тесте дефолтов; шпион подтвердил, что значение
из YAML доходит до пакета, а не подменяется дефолтом.

Запущен повторный сухой прогон на всей эталонной записи, с проверкой отпечатка
кода уже в живом пайплайне: правка в `transcript_assembly` → пересчитывается
только `merge`.

### 8.8 Повторный сухой прогон на всей записи — P0/P1 нет

| Что | Прогон 1 (8.3) | Прогон 2 |
|---|---|---|
| Результат | `transcript.md`, код 0 | `transcript.md`, код 0 |
| Время | 355 с | 360 с (frames 5 мин 18 с, slides 37.7 с, остальное < 0.5 с) |
| Пик RAM | 1.09 ГБ | 1.15 ГБ |
| Секции слайдов | 53 | **51** — две группы: «Слайды 19–20» (одна ссылка, PNG побайтово равны), «Слайды 21–22» (две ссылки, PNG разные) |
| «Речь вне слайдов» | 45 (44 короче 15 с) | **1** — вступление 26 с |
| Ссылки на PNG | 53, все разрешаются | 52, все разрешаются |
| Полнота слов | 12 488 = 11 377 + 1 111 | то же, все 1942 метки по порядку |
| Двойная пометка / `MM:SS` / строчные начала / нет длительности | 18 / 128 / 621 / да | **0 / 0 / 0 / длительность есть** |

**Отпечаток кода в живом пайплайне подтверждён:** второй прогон — попадание
по всем 9 стадиям; после добавленной строки `#` в `paragraphs.py` пересчиталась
**только** `merge`; файл возвращён побайтово (хэш и `cmp`); четвёртый прогон —
снова полное попадание. `transcript.md` одинаков байт в байт во всех четырёх.

Остаток формата — в `TECH_DEBT.md`, главное: первая строка OCR в заголовке
повторяется первой строкой врезки.


## 11. Итог

**Финальный прогон тестов (главная сессия, после остановки всех агентов):**

- быстрый набор `-m "not gpu and not models and not slow"`: **587 passed**;
- медленные тесты на эталонной записи `-m "slow and not gpu and not models"`: **24 passed** (13 мин 46 с);
- не запускались: тесты с маркерами `gpu` и `models` — нужны RTX 4060 и веса моделей.

**Задачи OpenSpec:** 28 из 45 отмечены. Остальные 17 требуют целевой машины
или весов моделей — таблица в разделе 9.

**Что работает и проверено на всей эталонной записи** (сухой прогон, 8.3 и 8.8):
`mp4 → transcript.md` через все 9 стадий, кэш с отпечатком кода, аудио-режим,
детерминированность, полнота слов, формат под конспект. Не проверено: качество
распознавания OCR и речи, VRAM и время на GPU.

**Путь на целевой машине:**

1. `docker compose -f docker/compose.yaml build`
2. `docker/verify_env.sh` внутри контейнера → exit 0 (задачи 1.1, 1.2)
3. `python -m lecture_transcript.warmup` — скачать веса
4. `pytest -m models` — тесты 4.2, 4.4, 5.3, 5.4
5. `lecture-transcript wr_20260909_1150.mp4 out/` → задачи 7.1–7.6

**Артефакты:** `DECISIONS.md` (этот файл), `TECH_DEBT.md`, `tools/dry_run.py`.
Код не закоммичен — `git init` сделан, коммитов нет, как и просили правила.

