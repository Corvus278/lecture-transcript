## 1. Окружение и каркас

- [ ] 1.1 Собрать Docker-образ с CUDA-runtime и ffmpeg с поддержкой nvdec; проверить, что `nvidia-smi` внутри контейнера видит RTX 4060 и `ffmpeg -hwaccels` перечисляет `cuda`
- [ ] 1.2 Зафиксировать зависимости Python с версиями; проверить, что установка в чистом образе проходит без конфликтов и torch видит GPU
- [x] 1.3 Создать структуру пакета по возможностям (`media_ingest`, `slide_extraction`, `slide_ocr`, `speech_transcription`, `transcript_assembly`) и точку входа CLI; проверить, что `--help` печатает параметры выбора ASR- и OCR-бэкенда
- [x] 1.4 Реализовать слой кэша промежуточных артефактов с ключом по входному файлу и параметрам этапа (design D8); проверить unit-тестом, что смена параметра инвалидирует только свой этап и более поздние
- [x] 1.5 Положить эталонную запись `wr_20260909_1150.mp4` в фикстуры (или зафиксировать путь к ней в конфиге тестов) и нарезать короткие фрагменты для быстрых тестов; проверить, что фрагменты содержат интервал со слайдом, интервал без демонстрации и момент переключения слайда

## 2. Приём медиа (media-ingest)

- [x] 2.1 Реализовать извлечение аудио в WAV 16 кГц моно со сведением каналов; проверить на эталонной записи, что длительность выхода совпадает с длительностью входа и частота равна 16 кГц
- [x] 2.2 Реализовать выбор аудиодорожки при нескольких дорожках с логированием выбора; проверить на синтетическом файле с двумя дорожками, что выбирается заданная, а по умолчанию первая
- [x] 2.3 Реализовать извлечение кадров с настраиваемой частотой и временны́ми метками через nvdec; проверить, что для эталонной записи при 1 fps число кадров соответствует длительности, а метки монотонны
- [x] 2.4 Реализовать определение состава дорожек и обработку отсутствующих: аудио-режим без видео, ошибка без аудио, ошибка на нечитаемом файле; проверить тестами на трёх подготовленных файлах, что частичные выходные артефакты не создаются

## 3. Извлечение слайдов (slide-extraction)

- [x] 3.1 Реализовать расчёт per-pixel дисперсии и яркости по выборке кадров в скользящем окне (design D3); проверить на кадре эталонной записи со слайдом, что маска низкой дисперсии и высокой яркости покрывает область демонстрации и не покрывает тайлы участников
- [x] 3.2 Реализовать выделение крупнейшего связного прямоугольника области демонстрации и детект её отсутствия; проверить на трёх кадрах эталонной записи (t≈10:00 со слайдом, t≈40:00 без демонстрации, t≈70:00 со слайдом), что границы найдены в первом и третьем случае и отсутствие определено во втором
- [x] 3.3 Реализовать отслеживание смены раскладки и фиксацию области демонстрации на интервалы стабильной раскладки; проверить на эталонной записи, что переходы «демонстрация вкл/выкл» найдены и их число совпадает с ручным подсчётом
- [x] 3.4 Реализовать perceptual hash кропа области демонстрации и группировку последовательных кадров по порогу hamming distance; проверить на интервале эталонной записи с дописыванием маркером, что кадры остаются в одной группе
- [x] 3.5 Реализовать выбор репрезентативного кадра как последнего в группе (design D4); проверить на слайде «Пример 2», что выбранный кадр содержит дописанный маркером ответ, а не пустую заготовку
- [x] 3.6 Реализовать сохранение кропов слайдов в PNG со стабильными именами, упорядоченными по времени, и выдачу списка слайдов с интервалами; проверить, что интервалы упорядочены, не пересекаются, а повторный прогон даёт те же имена файлов
- [x] 3.7 Подобрать порог hamming distance на эталонной записи и зафиксировать в конфиге; проверить, что число логических слайдов совпадает с ручным подсчётом по записи

## 4. Распознавание слайдов (slide-ocr)

- [x] 4.1 Определить контракт OCR-бэкенда (изображение → Markdown-фрагменты с confidence) и реестр бэкендов с проверкой доступности до начала прогона; проверить тестом, что выбор несуществующего бэкенда падает сразу с внятным сообщением
- [ ] 4.2 Реализовать бэкенд распознавания печатного текста с выдачей боксов строк и confidence; проверить на слайде «Как называются эти числа …», что текст распознан без ошибок
- [x] 4.3 Реализовать роутинг строк в распознавание формул по математическим символам и низкой уверенности (design D5); проверить, что строка `√(x²−x) = −5` уходит в формульный распознаватель, а строка «Пример 2.» — нет
- [ ] 4.4 Реализовать бэкенд распознавания печатных формул в LaTeX; проверить на слайде «Пример 2», что результат рендерится в корректную формулу
- [x] 4.5 Реализовать сборку результата слайда в Markdown с сохранением порядка строк и обязательной ссылкой на PNG; проверить, что ссылка присутствует даже для слайда без распознанного содержимого
- [x] 4.6 Реализовать маркировку неуверенных фрагментов и пометку слайда как ненадёжного; проверить на искусственно размытом кропе, что пометка выставляется
- [ ] 4.7 Реализовать сборку глоссария терминов по всем слайдам с дедупликацией и отсечением повторяющихся служебных надписей оформления; проверить на эталонной записи, что в глоссарий попали математические термины и не попал логотип платформы
- [x] 4.8 Добавить альтернативный OCR-бэкенд на базе VLM за тем же контрактом (design D5, опция); проверить, что переключение флагом даёт результат в том же формате
- [ ] 4.9 Проверить отсутствие сетевых вызовов на этапе распознавания слайдов; проверить прогоном в контейнере без сети, что этап завершается успешно

## 5. Распознавание речи (speech-transcription)

- [x] 5.1 Определить контракт ASR-бэкенда (аудио + опциональный глоссарий → слова с таймкодами, флаг наличия своей пунктуации) и реестр с проверкой доступности; проверить тестом, что недоступный бэкенд отвергается до начала прогона
- [x] 5.2 Реализовать детекцию речи с отсечением длительной тишины и сохранением абсолютных таймкодов; проверить на фрагменте с паузами, что слова после паузы имеют корректное время относительно начала записи
- [ ] 5.3 Реализовать ASR-бэкенд для русской речи с передачей глоссария как hotwords; проверить на фрагменте эталонной записи, что термины из глоссария распознаны в написании глоссария
- [ ] 5.4 Реализовать ASR-бэкенд для смешанной речи с сохранением латиницы; проверить на фрагменте с англоязычным термином, что термин не транслитерирован
- [x] 5.5 Реализовать деградацию при бэкенде без поддержки глоссария — прогон без него и предупреждение; проверить тестом, что предупреждение выдано и прогон успешен
- [x] 5.6 Реализовать стадию восстановления пунктуации и регистра, включаемую только для бэкендов без своей пунктуации; проверить, что таймкоды слов после стадии совпадают с таймкодами до неё (design D6)
- [x] 5.7 Проверить единство формата выхода между бэкендами; проверить одним тестом, прогоняющим оба бэкенда на одном фрагменте, что схема результата идентична и времена́ монотонны
- [ ] 5.8 Проверить отсутствие сетевых вызовов на этапе распознавания речи; проверить прогоном в контейнере без сети, что этап завершается успешно

## 6. Сборка транскрипта (transcript-assembly)

- [x] 6.1 Реализовать слияние слов и слайдов по пересечению интервалов с отдельными секциями для речи вне слайдов; проверить тестом на полноту — множество слов в транскрипте совпадает с множеством слов из ASR
- [ ] 6.2 Реализовать допуск на границах секций, не разрывающий фразы; проверить на моменте переключения слайда эталонной записи, что фраза через границу попала в одну секцию целиком
- [x] 6.3 Реализовать чистку филлеров и оборванных самоповторов по консервативным правилам; проверить на отобранных фрагментах, что заполнители удалены, а содержательные формулировки сохранены
- [ ] 6.4 Реализовать разбиение речи на абзацы по смысловым границам; проверить на десятиминутном монологе эталонной записи, что текст не одним блоком и не разбит по каждой паузе
- [x] 6.5 Реализовать шаблон вывода `transcript.md`: заголовки секций с номером слайда и интервалом, врезка содержимого слайда, ссылка на PNG, промежуточные таймкоды внутри длинных секций; проверить, что вывод рендерится в Markdown с формулами и картинками
- [x] 6.6 Обеспечить детерминированность сборки; проверить двумя прогонами на одних входных данных, что файлы совпадают побайтово

## 7. Сквозная проверка

- [ ] 7.1 Прогнать весь пайплайн на эталонной записи целиком; проверить, что `transcript.md` создан, содержит секции по слайдам, формулы и ссылки на PNG
- [ ] 7.2 Замерить время каждой стадии и пиковый расход VRAM; проверить, что суммарное время в пределах ~15 минут и пик VRAM ниже 8 GB (design D7)
- [ ] 7.3 Прогнать пайплайн с альтернативными бэкендами (Whisper вместо GigaAM, VLM вместо OCR-пары); проверить, что прогон завершается, пик VRAM остаётся ниже 8 GB и формат вывода не изменился
- [ ] 7.4 Проверить выгрузку моделей между стадиями; проверить по замерам, что VRAM освобождается после каждой стадии, а не накапливается
- [ ] 7.5 Прочитать итоговый транскрипт как исходник для конспекта и зафиксировать замечания по формату; проверить, что найденные проблемы формата описаны для следующей итерации
- [ ] 7.6 Подобрать значения из Open Questions в `design.md` (допуск на границах секций, порог confidence OCR) на эталонной записи и зафиксировать в конфиге; проверить, что значения задокументированы с обоснованием
